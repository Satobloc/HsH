# Meridian Run 029 — 2026-09-17

## 00:25 EDT — extraction-state check
Read controlling Common surfaces and checkpoint. Checked `WORKSPACES/COMMON/extraction_outputs/` for the requested second bounded window of `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_3/26.03.13•26.04.21•Ontology and Math Check — raw.json`. Confirmed the first 60-user-message extraction exists as `meridian_ontology_math_check_opening_20260916.{json,md}`. Confirmed request `WORKSPACES/COMMON/extraction_requests/meridian_ontology_math_check_window2_20260916.json` exists with `after_create_time: 1773400614.589785`, `user_limit: 60`, `context_each_side: 1`. Corresponding output was not yet present. No reconstruction from memory and no duplicate extraction request.

## 04:25 EDT — direct-source recovery supersedes extraction wait

### Startup / controls
Re-read `NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, `CURRENT_HSH_HYPOTHESIS_STATUS_2026-09-14.md`, current `COORDINATION.md`, `HANDOFFS.md`, and `TRIAL_CHECKPOINT.md`; also re-read `SHARED_STATE_WRITE_SAFETY.md` and `CROSS_REPO_SCRIPT_EXECUTION_STANDARD.md` before durable write. No conversation-title action. Integration-lane handoffs remain suspended.

### Exact source / coverage
Library retrieval now exposes the exact body of `Ontology and Math Check — raw.json` directly. Targeted exact-text recovery covered Nathan's full Donut/`Curvy_Lisa` message at create_time `1773482504.984154`, Nathan's later named `Whirlygig` codification request at `1773519926.437671`, and the immediately following assistant simplification/formalization. This makes the pending bounded extraction unnecessary for this specific source question; do not issue another request merely to duplicate now-accessible content.

### Material source result — full Donut
Nathan defines closed curves `R` and `Q` on a sphere; anchors `i_r` and `i_q`; a right-angle/rotating-plane construction; self-intersecting Great Annulum `GrAnn`; chord `i_ri_q`; midpoint `W_obble`; perpendicular plane `W_iggle`; rotational vector `V`; and the enclosing Donut with `P_ivot`, `Donomphulon`, and `BigRad`. `P_ivot` is driven simultaneously along the torus outer-radius circuit and `GrAnn`, with an even-multiple circuit constraint. Nathan states that `V` sweeps `Curvy_Lisa` on the Donut and that this defines `QwrtR`, called the derivative of Q with respect to R.

This is the strongest recovered Nathan-origin mechanical specification so far. Preserve the derivative interpretation as an intended claim, not an independently established mathematical result.

### Material source result — named Whirligig reduction
Later Nathan asks to codify the `Whirlygig` as a complex sphere inside a torus taking two equations, mapping them to hemispherical curves, and using pins/rotational axes to draw a composite Lissajous trace. The assistant responds with a much simpler model: two parametric functions drive orthogonal sphere rotations and feed standard torus coordinates; a later assistant message adds optional linear couplings `phi=x+k1*y`, `theta=y+k2*x`.

### Representation/equivalence result
No explicit reduction, quotient, or equivalence map is supplied between the full Donut mechanism and the simplified two-angle torus map. In particular, the reduced model does not encode the Great Annulum, anchor/intersection constraints, chord-midpoint dynamics, moving perpendicular plane, vector sweep, `P_ivot`/`Donomphulon`/`BigRad`, or the even-multiple winding condition.

Conservative typing is therefore:

`full Donut mechanism -> later simplified/named Whirligig surrogate`

with equivalence **OPEN**, not assumed.

Assistant claims that the full scaffold automatically supplies a mathematically well-defined derivative, holonomy preservation, 4D UI projection, or exact derivation between equations remain assistant extrapolations unless an operator-level derivation is recovered elsewhere.

### Benchmark gate / intervention budget
A faithful full-vs-reduced numerical benchmark cannot yet be run without silently inventing missing rules. Minimum unresolved operator data include: exact embeddings/parameterizations of R and Q; precise equation/geometry of `GrAnn`; evolution laws for the rotating planes; the ambiguous `i_ri_c`/chord-normal reference in the `W_iggle` sentence; angular law for `V`; exact coupling among `P_ivot`, sphere rotation, `Donomphulon`, and `BigRad`; and a rule selecting the relevant intersection of V with the torus if multiple intersections exist.

Any reconstruction that chooses these locally must charge those choices to the intervention budget and cannot be used as evidence that the historical full and reduced representations are invariant/equivalent.

### Exposure / sandbox / archive state
Source archaeology and representation audit only; no direct theory construction or repair. No nLab, PRIOR_ART, quarantine, external literature, or suspended Integration handoff. Direct Library access has removed the previous retrieval blocker for this source and should be preferred over waiting on duplicate derived extraction when exact source text is already retrievable.

### Capability/enrichment
Improved retrieval-path discrimination and representation typing: descendant simplification is now explicitly separated from equivalence, and missing completion rules are enumerated before any numerical reconstruction.

### Current frontier / best next operation
Inspect the Nathan messages immediately following the full Donut construction for explicit clarifications of the missing operator data before attempting reconstruction. If those data are never supplied, freeze the full Donut as partially specified and benchmark only explicitly defined suboperators rather than manufacturing completion rules.

### Handoffs / Nathan action
None. No Nathan action required.
