# Meridian run 013 — 2026-09-15 10:30 EDT

**Scheduled phase:** :28  
**Status:** source-first solver archaeology; no theory promotion; quarantine/nLab blind.

## Startup/control coverage

Read current `NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, `CURRENT_HSH_HYPOTHESIS_STATUS_2026-09-14.md`, `COORDINATION.md`, `HANDOFFS.md`, `BULLETIN_BOARD.md`, and `WORKSPACES/MERIDIAN/TRIAL_CHECKPOINT.md`. Newer autonomy control supersedes the stale Bulletin Board standdown banner. No conversation title was altered or suggested for alteration.

## Exact source coverage

Continued sequential/source-first audit of:

`DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_16/QM and Relativity Connection — raw.json`

Conversation ID: `69ac5fbe-b534-832e-bbf0-a7f0635ab876`.

Read the March active branch through the Python UI templates and the later empty-body attachment/context turn boundary. This run focused on whether the historical implementation supplies the missing lift/gauge rule or a control-history metric identified in RUN_012.

## Material result — historical code does not implement the claimed R(lambda) extraction

The assistant explicitly says the framework will take a parametric trajectory, map it to a unit 4D hypersphere, and **extract scale r(lambda) and rotation R(lambda)**. But the code's `map_to_ui` returns only:

- `r_vals = ||traj||`, and
- `unit_traj = traj / r_vals`.

No `R(lambda)` is constructed or returned.

The next routine, `apply_4d_rotation`, applies a user-chosen fixed rotation to the normalized trajectory. It therefore creates a transformed trajectory; it does not recover the source trajectory's SO(4) control history.

The symbolic-input extension repeats the same substitution: normalize to scale + direction, then apply fixed plane rotations. It likewise does not solve the inverse lift from direction on S^3 to an SO(4) frame history.

This strengthens RUN_012's underdetermination diagnosis. The historical implementation silently replaces the claimed inverse operator `curve -> (r(lambda), R(lambda))` with `curve -> (r(lambda), direction(lambda))`, then appends arbitrary chosen rotations. The SO(3) stabilizer/gauge freedom remains unresolved in this source.

## Additional implementation defect

The symbolic extension's sequential 1–3 plane update mutates `traj_rot[:,0]` and then uses that already-updated value when computing `traj_rot[:,2]`. As written, this is not the standard simultaneous 2D rotation update in the (1,3) plane. This is a code-level defect in the historical example, separate from the conceptual gauge problem.

## Path-integral/equivalence status

Nathan proposes integrating possible particle paths after inserting a reactive object/interaction. The assistant labels this a deterministic geometric path integral and asserts equivalence to standard path integration, but the inspected source still supplies no amplitude/phase weight, measure on path space, normalization, or rule mapping geometric accumulation to quantum amplitudes. Preserve this as an assistant-authored interpretation/claim, not an established UI operator.

## Benchmark/exploration result

A clean historical-vs-required-operator distinction is now available:

- **claimed historical inverse:** `y(lambda) -> (r(lambda), R(lambda))`;
- **implemented inverse:** `y(lambda) -> (||y||, y/||y||)`;
- **added transform:** arbitrary fixed plane rotations chosen by the program/user;
- **missing:** unique/gauge-fixed SO(4) lift, transported frame rule, or representation-invariant quotient; and a metric/action on control histories sufficient to define a shortest derivation.

This is a negative result and a useful regression fixture. A future reconstruction should first reproduce this historical behavior exactly, then separately introduce any gauge repair in sandbox with explicit provenance.

## Exposure/cross-reading state

No nLab, PRIOR_ART, quarantine, external literature, current Kerr/Kelvin construction, or suspended Integration handoff consumed. Cross-reading is confined to the historical conversation's own user/assistant/code record and current Common controls.

## Archive/infrastructure change

Created this durable run record rather than risking whole-file replacement of the long checkpoint through a truncated connector response. No shared semantic/control surface modified.

## Enrichment/capability change

Meridian now has a sharper operator audit criterion: distinguish a point on S^3 from a lifted SO(4) frame history and test whether code advertised as extracting controls actually returns the claimed control object.

## Failures/uncertainties

- No lift/gauge convention found in this conversation.
- No control-history metric/action found in the inspected implementation.
- Absence here does not establish absence in later UI/Whirligig/Hagalaz sources.
- Historical source may contain branches outside the active March chain; no claim about uninspected branches.

## Current frontier / best next operations

1. Move chronologically from this March 7 UI source toward the March 16 Whirligig packet, looking for an explicit SO(4) lift/gauge convention or control-history metric.
2. If absent, inspect later firsthand Hagalaz/UI/TX source history for whether Hagalaz supplies the missing quotient/operator rather than assuming equivalence.
3. Freeze a minimal representation-invariance benchmark: same S^3 curve, multiple admissible SO(4) lifts differing by stabilizer rotations; any curve-level invariant should be unchanged.
4. Preserve historical code defects and underdetermination separately from later sandbox repairs.

**Nathan attention required:** no.
