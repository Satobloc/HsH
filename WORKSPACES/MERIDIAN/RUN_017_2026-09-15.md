# Meridian Run 017 — 2026-09-15

**Actual start:** 2026-09-15 18:26 EDT
**Lane:** source-first solver/representation archaeology; archive provenance
**Sandbox/theory status:** no direct theory construction performed

## Startup / control coverage
Read current `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, `CURRENT_HSH_HYPOTHESIS_STATUS_2026-09-14.md`, current Coordination/Handoffs surfaces available at their canonical paths, current Meridian checkpoint, and Sable's current automation/project-systems checkpoint. Newer Nathan directives control. No conversation/thread/chat was renamed, retitled, altered, or proposed for renaming.

## Operation
Continued provenance work on `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_1/02_whirlygig_torus_trace_raw.png` after prior runs established only its repository-upload boundary (2026-09-05) and found no indexed textual filename/stem hits.

Used the GitHub connector's binary-safe `fetch_file(..., encoding=base64)` path rather than inferring image content from filename/co-location. The returned PNG header is sufficient to recover embedded metadata before the response truncates in the image payload.

## Material result
The PNG itself embeds a `Software` text chunk identifying **Matplotlib version 3.10.7** (`https://matplotlib.org/`). Its IHDR reports **1440 × 280 pixels**, RGBA/truecolor-with-alpha (PNG color type 6), 8-bit depth. A pHYs chunk is also present at 6299 pixels/meter in both axes (approximately 160 dpi if interpreted as physical resolution).

This is the first artifact-internal provenance evidence recovered for the specimen. It establishes that the archived file was produced/exported through Matplotlib 3.10.7, not merely that it happens to be named `whirlygig_torus_trace_raw.png` or sits beside Whirligig conversation exports. It still does **not** establish creation date, author, generating script/notebook, semantic relation to the March 2026 Whirligig work, or whether the plotted trace is historical output versus later reconstruction.

## Exposure / cross-reading
No nLab, PRIOR_ART, quarantine, or external literature opened. The PNG payload was accessed only far enough to read its own embedded PNG metadata; no visual/theory interpretation was performed this run. This preserves a useful separation between artifact-internal provenance and later visual interpretation.

## Archive / infrastructure result
Binary solver artifacts can carry useful provenance unavailable to text/code search. Add PNG metadata/chunk inspection to the solver-source archaeology method before semantic interpretation. For this specimen, preserve the following evidence layers separately:
1. repository placement/name;
2. repository upload/commit boundary;
3. artifact-internal metadata (now: Matplotlib 3.10.7, dimensions/resolution);
4. generating-code/text provenance (still missing);
5. visual/semantic interpretation (not yet performed).

## Enrichment / capability change
Established a binary-safe GitHub retrieval route using `fetch_file` with base64 encoding; this avoids the UTF-8-only limitation of generic GitHub fetch for repository images and allows metadata-first inspection without immediately consuming the visual specimen.

## Failures / uncertainties / blockers
Repository code search remains negative for the distinctive filename/stems. Generic GitHub fetch rejects the PNG as non-UTF-8; direct raw download is unavailable in the container environment. The binary-safe connector response truncates during the large IDAT payload, so no full local image reconstruction was possible this run. No Nathan action is required.

`TRIAL_CHECKPOINT.md` currently exposes only a partial whole-file payload through the connector while GitHub writes require complete replacement; overwriting it from a truncated read would risk destroying unseen checkpoint state. This run is therefore recorded durably here rather than performing an unsafe checkpoint replacement.

## Current frontier / best next operations
1. Search source/code history for Matplotlib-generation clues rather than filename alone: distinctive plot dimensions, likely `savefig` naming, and nearby scripts/notebooks.
2. If textual provenance remains absent, obtain/inspect the full image through a binary-capable path and classify visible labels/geometry strictly as artifact evidence, not source-history inference.
3. Preserve any recovered generating script as a separate representation/version specimen and test whether its plotted trace corresponds to the historical UI/Whirligig operator or a later reconstruction.

**Nathan attention required:** no.
