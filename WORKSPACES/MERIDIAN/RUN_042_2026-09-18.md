# Meridian Run 042 — 2026-09-18

**Start:** 2026-09-18 00:26 EDT
**Status:** MATERIAL / sandboxed mathematical classification

## Startup / controls
Read `NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, `CURRENT_HSH_HYPOTHESIS_STATUS_2026-09-14.md`, current `COORDINATION.md`, `HANDOFFS.md`, `SHARED_STATE_WRITE_SAFETY.md`, and current Meridian checkpoint. Integration-lane handoffs remain suspended. No conversation-title action taken.

## Exact source coverage
Primary mathematical source: `rotoexpando_geometry_definition.tex` (11 Sep 2026), especially nested-order atlas, framed-sphere/UI definition, Hagalaz similarity datum, equivalent-representation claim, toroidal encoding/Whirlygig reduction, and immediate mathematical program.

The manuscript defines the adjacent-order atlas
`C_n=(mu_n,nu_n,xi_n,Delta phi_n,Q_n,chi_n,a_n/D_n)`;
framed spheres `S_i=(c_i,r_i,F_i)`; and bare Hagalaz as `H_ij=(sigma_ij,Q_ij)` or, retaining translation, `(Delta c_ij,sigma_ij,Q_ij)`, with similarity map `x -> c_j + sigma_ij Q_ij (x-c_i)`.

## Operation — minimal injectivity / information-loss benchmark
Freeze two admissible adjacent-order states with identical framed-sphere similarity data but different dynamic/topological atlas data:

`C_A=(mu, nu_A, xi, Delta phi_A, Q, chi_A, epsilon)`
`C_B=(mu, nu_B, xi, Delta phi_B, Q, chi_B, epsilon)`

Choose, for a concrete specimen,
`mu=2`, `xi=1/4`, `Q=I`, `epsilon=a/D=10^-2`, while
`nu_A=3/2`, `Delta phi_A=0`, `chi_A=+1`,
`nu_B=sqrt(2)`, `Delta phi_B=pi/7`, `chi_B=-1`.
Use the same centers, radii ratio, and instantaneous frames for both states.

Then by the manuscript's own definition both map to exactly the same bare Hagalaz datum:
`H_A = H_B = (Delta c, 2, I)`.
Yet the atlas states differ in phase-rate ratio, phase, and chirality. Therefore the map from the manuscript's full candidate atlas to bare Hagalaz is non-injective unless those quantities are encoded elsewhere in an enriched/time-dependent frame history.

## Result
This is a clean negative representation-invariance result, not a failure of Hagalaz as a similarity operator:

1. **Bare Hagalaz is an exact relative-similarity operator, not by itself a lossless encoding of the full candidate atlas.**
2. The manuscript's broad `equivalent/losslessly convertible` representation statement is valid only with the explicit qualifier already present in the text: *provided the required frame data are retained*. For dynamic helix/phase information, an instantaneous `(Delta c,sigma,Q)` is insufficient.
3. The missing-data/intervention budget is now concrete. To reconstruct the full atlas from Hagalaz one must additionally supply or derive at least phase-rate information `nu`, relative phase `Delta phi`, chirality `chi`, and finite-core thickness `a/D` unless a declared history/convention embeds them in `Q(s)`, its derivatives, or another attached channel.
4. Toroidal winding is potentially richer than bare Hagalaz for this benchmark: signed winding slope `q=dpsi/dphi` can encode a phase-rate ratio/chirality under an explicit orientation convention, and initial phase offsets can encode `Delta phi`; however centerline torus coordinates still do not encode finite-core thickness unless thickness is retained as an additional field/tube radius. No Whirligig equivalence is credited until the exact transformation is written.
5. Three Spheres -> UI -> bare Hagalaz may therefore be a legitimate projection/reduction while still losing dynamic atlas information. This sharpens, rather than contradicts, the source statement that Three Spheres is a constrained UI configuration and Whirligig's exact reduction status remains to be established.

## Benchmark classification
**PASS:** reproducible counterexample establishes non-injectivity of full-atlas -> instantaneous bare-Hagalaz map under the manuscript's current definitions.
**NOT TESTED:** whether a *time-dependent/enriched* Hagalaz history `(Delta c(s),sigma(s),Q(s))` plus declared derivatives/conventions can recover `nu,Delta phi,chi`; whether Donut/Whirligig preserves the same observables; exact helix -> framed-sphere map.

## Sandbox / exposure
Internal source-first mathematical test only. No external literature, nLab, PRIOR_ART, quarantine, or Kerr/Kelvin construction used. No physical claim promoted.

## Archive / infrastructure consequence
Solver canon should type `Hagalaz-static` separately from any future `Hagalaz-history/enriched` representation. Do not label conversion `lossless` without listing retained channels and an inverse/reconstruction rule.

## Enrichment / capability change
Added a minimal injectivity test as a reusable representation benchmark: construct two source-admissible states differing only in purportedly preserved variables and test whether the representation map identifies them. This is stronger and cheaper than visual trace comparison.

## Failure / uncertainty
The current checkpoint is too large for the connector to return completely; whole-file replacement would violate shared-state write safety. This run artifact therefore preserves the durable state, but the checkpoint itself was not replaced. This is an infrastructure limitation, not a mathematical blocker.

## Current frontier / next operation
Test the smallest enriched Hagalaz history needed to recover `nu`, `Delta phi`, and `chi` from a canonical nested-helix pair. Explicitly distinguish quantities recoverable from `Q(s)`, quantities requiring `dQ/ds`/phase parameterization, and quantities (`a/D`) requiring an independent finite-core channel. Only then compare that enriched state against toroidal/Whirligig encoding.

## Handoffs / Nathan action
No Nathan action required. No workflow redesign proposed.