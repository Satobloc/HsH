# Hagalaz framed-hypersphere representation test

**Worker:** Meridian Solver Loop  
**Date:** 2026-09-20  
**Task/branch:** Hagalaz/UI/Three-Spheres representation formalization  
**Status:** CLAIMED VERIFIED for the narrow representation identities tested here; not a physical claim and not a validation of Hagalaz dynamics

## Scope

This is the bounded computational follow-up to `HAGALAZ_FRAMED_HYPERSPHERE_NOTE_2026-09-20.md`. It tests only the coordinate representation

\[
\mathcal S_i=(c_i,D_i,F_i;\mathcal P_i),\qquad
ᚻ_{ij}=(\Delta c_{ij},\sigma_{ij},Q_{ij}),
\]

with

\[
\Delta c_{ij}=c_j-c_i,\quad \sigma_{ij}=D_j/D_i,\quad Q_{ij}=F_jF_i^{-1}.
\]

The test used three randomly generated points in \(\mathbb R^4\), three positive scales, and three random proper orthogonal \(4\times4\) frames generated by QR decomposition. A fixed RNG seed (`20260920`) makes the numerical instance reproducible. Errors below are Euclidean/Frobenius norms or absolute scalar residuals.

## Results

| Check | Residual |
|---|---:|
| three-node displacement closure \(\Delta c_{12}+\Delta c_{23}+\Delta c_{31}=0\) | `5.09e-16` |
| scale closure \(\sigma_{12}\sigma_{23}\sigma_{31}=1\) | `1.11e-16` |
| frame closure \(Q_{31}Q_{23}Q_{12}=I\) | `1.04e-15` |
| global-translation invariance of \(\Delta c_{ij}\) | `4.97e-16` |
| common-rescaling invariance of \(\sigma_{ij}\) | `4.44e-16` |
| global-frame-rotation covariance \(Q'_{ij}=RQ_{ij}R^{-1}\) | `3.75e-16` |
| affine-similarity translation composition | `7.38e-15` |
| direct displacement composition \(\Delta c_{12}+\Delta c_{23}=\Delta c_{13}\) | `5.09e-16` |

All residuals are consistent with floating-point roundoff for this test instance.

## Important distinction confirmed

The center-displacement coordinate and affine-similarity translation coordinate are different objects.

For a similarity map

\[
T_{ij}(x)=\sigma_{ij}Q_{ij}x+b_{ij},
\]

that sends center \(c_i\) to center \(c_j\),

\[
b_{ij}=c_j-\sigma_{ij}Q_{ij}c_i.
\]

Direct center displacement composes additively:

\[
\Delta c_{13}=\Delta c_{12}+\Delta c_{23}.
\]

By contrast, affine translation composes as

\[
b_{13}=\sigma_{23}Q_{23}b_{12}+b_{23},
\]

when \(T_{23}\circ T_{12}\) is used. The numerical residual for that law was `7.38e-15`. Therefore a future Hagalaz implementation must type these coordinates separately rather than calling both simply “translation.”

## Frame behavior

With frames stored in a common ambient basis, a global ambient rotation \(R\) sends \(F_i\mapsto RF_i\), hence

\[
Q_{ij}\mapsto RQ_{ij}R^{-1}.
\]

So the matrix entries of \(Q_{ij}\) are not strictly invariant under global ambient rotation; they transform by conjugation. Conjugacy invariants (for example eigenvalue/rotation-plane data, when appropriately typed) are candidates for basis-independent descriptors. A co-rotating storage convention could instead make the recorded relative matrix numerically unchanged, but that is a representation convention and should be declared explicitly.

## Verification status

Under `LLM_MATH_PROVENANCE_VERIFICATION_PROTOCOL.md`, this packet is **CLAIMED VERIFIED** only for these elementary representation identities: an LLM-generated computational pass checked them numerically, and each also follows directly from the stated definitions. This is not `MULTIPLY VERIFIED`; no independent second pass is recorded here. It establishes no H(s)H dynamics, physical equivalence, empirical fit, or superhelical law.

## Durable consequence

The minimal Hagalaz representation can safely proceed with three externally typed relation coordinates:

1. center displacement \(\Delta c\);
2. positive scale ratio \(\sigma\);
3. relative frame rotation \(Q\);

while keeping internal plumbing \(\mathcal P\) separate. UI remains the \(\Delta c=0\) constraint slice; Three Spheres can use the same pairwise relation type without building center-pinning into the base object.

## Next cursor

Excavate the new `DEVELOPMENT_FULL_CONVOS/HAGALAZ/` corpus specifically for the historical definitions of the omitted/opened Hagalaz degree of freedom and for UI internal variables that should populate \(\mathcal P\). Do not infer those from the present representation test.
