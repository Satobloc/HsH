# Meridian Run 065 — 2026-09-19

**Actual start:** 2026-09-19 11:30 EDT

## Preflight
Reread current `NO_CONVERSATION_RENAMING_POLICY.md`, preflight/must-read controls in `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md` and `AUTOMATION_WORKFLOW_CONTROL.md`, `CURRENT_HSH_HYPOTHESIS_STATUS_2026-09-14.md`, current `COORDINATION.md` / `HANDOFFS.md`, Sable README plus current direction-unification priorities, and Meridian checkpoint / Run 064 cursor. Signet rule observed. `SHARED_STATE_WRITE_SAFETY.md` reread immediately before repository write. Quarantine not entered.

## One-object operation
Continued Run 064's exact-operator audit of the legacy `[LEGACY SAT / PRE-H(s)H / exact source-conversation date not yet recovered]` Donut construction in `_AUTO_EXTRACTED_TEXT/FINDING THE DONUT (nolat).txt`, specifically the named output `Curvy_Lisa` and asserted `QwrtR` = “the derivative of Q wrt R.” Supporting search only: exact-identifier repository searches for `QwrtR` / `Curvy_Lisa` in accessible non-quarantined HsH / legacy surfaces. No external literature used.

## Result — the historical construction outputs a relational curve, not yet a typed derivative
The source construction described in Run 064 takes two closed curves `R` and `Q` on a sphere, builds intermediate geometric objects (`i_r`, `i_q`, rotating planes, Great Annulum `GrAnn`, chord midpoint `W_obble`, perpendicular plane `W_iggle`, vector `V`, and torus `The Donut`), and produces a Lissajous-like torus trace `Curvy_Lisa`. The source then declares that trace to define `QwrtR`, “the derivative of Q wrt R.”

On the displayed construction alone, the mathematical output is therefore typeable only as a **derived relational curve/trace on a torus** (more abstractly, a parameterized curve into the Donut surface once a parameterization is supplied). The source does not yet specify a standard derivative object or a new formal derivative operator.

For a conventional `dQ/dR` interpretation, additional structure is required. If `R` and `Q` are scalar functions of a common parameter `t`, then locally `dQ/dR = (dQ/dt)/(dR/dt)` where `dR/dt != 0`. If they are vector-valued curves on a sphere, there is no canonical scalar “derivative of Q with respect to R” merely from the pair of image curves: one needs at least a correspondence/common parameterization and a declared codomain/operator type (for example a tangent map/Jacobian-like object, relative tangent/phase observable, or a specifically SAT-defined relational operator). The Donut narrative as recovered does not state those data.

Accordingly, `Curvy_Lisa = QwrtR` is best source-typed at this stage as **historically asserted relational-derivative semantics**, not as a demonstrated ordinary derivative identity.

## Representation-invariance consequence
A useful exact benchmark now follows directly from the missing type information: hold the geometric images of `R` and `Q` fixed while changing their admissible parameterizations/correspondence. If `Curvy_Lisa` changes, the operator depends on representation/parameterization unless that parameterization is itself physical input. If it remains invariant, the source needs an explicit rule showing why. This is a sharper benchmark than asking whether the torus trace merely looks stable.

This benchmark does not assume that SAT intended an ordinary calculus derivative; it is precisely a discriminator between (a) standard derivative semantics, (b) a parameterization-dependent relational transform, and (c) a new invariant SAT-defined operator.

## Exact-identifier search result
Current GitHub code search returned no indexed hits for `QwrtR` or `Curvy_Lisa`, including a combined search. Because earlier Meridian runs established that GitHub code search can miss known raw-export content, this is **not absence evidence** and does not establish that the identifiers occur only in `FINDING THE DONUT`. It only means no later definition was recovered by this search method in this bite.

## Priority relation
The current direction-unification surface makes GR↔QM isomorphism P1. This Donut typing result feeds P1 directly: any claimed UI/Donut contribution to a genuine isomorphism must state the domain, codomain, map, inverse where claimed, and preserved operations/invariants. An untyped torus trace cannot by itself satisfy that obligation, though it may become a legitimate operator once its semantics and invariance are specified.

## Exposure / sandbox / quarantine
Source-first mathematical classification of a historical solver claim. No nLab, PRIOR_ART, quarantine, or external literature accessed. No new SAT/H(s)H theory promoted. The proposed reparameterization test is an audit benchmark, not a theory assertion.

## Archive / infrastructure
No source file altered. Version tag retained explicitly so legacy Donut semantics are not silently treated as current math. Exact-identifier code-search failure recorded as retrieval-method limitation rather than historical absence.

## Enrichment / capability
Sharpened operator typing: distinguish a curve-valued relational transform from a scalar derivative, tangent map/Jacobian, functional derivative, or invariant observable before testing equivalence claims.

## Failures / uncertainties
- Exact source-conversation date for `FINDING THE DONUT` remains unresolved.
- No later formal definition of `QwrtR` was recovered this bite.
- The historical source may intend `derivative` in a novel SAT-specific sense; that intent cannot be fixed without a later definition or attributable clarification.

## Current frontier / best next operation
**Next cursor:** recover a later executable or formal Donut/Whirligig specification and test the reparameterization benchmark on the actual map `(R,Q) -> Curvy_Lisa`. If no later map is found through chronology/catalog routing, freeze `QwrtR` as an untyped historical relational-derivative claim and return to the higher-priority GR↔QM map/inverse search.

**Nathan action:** none.
