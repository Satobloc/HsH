# Meridian run — 2026-09-16 18:26 EDT

## Scope
Source-first Whirligig/Donut reconstruction infrastructure. No quarantine, PRIOR_ART, nLab, or external-literature exposure. No conversation-title action.

## Startup/control coverage
Read `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, `CURRENT_HSH_HYPOTHESIS_STATUS_2026-09-14.md`, `COORDINATION.md`, `HANDOFFS.md`, `WORKSPACES/SABLE/README.md`, and `WORKSPACES/MERIDIAN/TRIAL_CHECKPOINT.md`. Newer Nathan directives control.

## Exact source / infrastructure coverage
Read the current bounded extraction implementation directly:
- `WORKSPACES/COMMON/scripts/extract_raw_window.py`
- `.github/workflows/extract-raw-window.yml`

Target source remains the chronology-resolved March 13 conversation:
`DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_3/26.03.13•26.04.21•Ontology and Math Check — raw.json`.

## Work completed
Created a bounded extraction request at:
`WORKSPACES/COMMON/extraction_requests/meridian_ontology_math_check_opening_20260916.json`

Request parameters:
- `after_create_time: 0`
- `user_limit: 60`
- `context_each_side: 1`

This deliberately takes a bounded opening bite rather than attempting to load or reconstruct the 813-message one-line raw export. The existing workflow is push-triggered on extraction-request changes and writes deterministic `.json` and `.md` extraction outputs with message IDs, roles, create times, parent IDs, and text.

## Result / current state
The request commit succeeded. The expected output file was not yet present on the immediate follow-up read, so the extraction workflow had not completed by the time of this run's check. This is a transient workflow/output wait, not a theory or Nathan blocker.

No operator equations were inferred from remembered excerpts. The full-Donut-vs-reduced-Whirligig representation benchmark remains gated on verbatim extraction of the historical specimens.

## Benchmark / theory / sandbox status
No direct theory construction. No equivalence claim. Existing benchmark gate remains unchanged: source-freeze both operator specimens before formalization or executable comparison.

## Archive/infrastructure change
Added the deterministic bounded extraction request needed to clear the large-raw-body access blocker. This is reproducible and leaves the original raw conversation untouched.

## Enrichment / capability change
Verified the extractor's exact semantics rather than assuming them: it sorts nonempty messages by create time, selects user messages after the requested cursor, includes bounded adjacent context, and emits stable message/timestamp metadata. This makes it suitable for source-first operator archaeology once the output lands.

## Failures / uncertainties
The workflow output had not appeared on immediate read. The current checkpoint is safely readable but connector presentation truncates the full replacement body; I did not risk a destructive partial-file replacement. This run note therefore preserves the complete state transition until a safe compare-and-swap checkpoint update can be made from a complete current body.

## Current frontier / best next operation
On the next run, fetch `WORKSPACES/COMMON/extraction_outputs/meridian_ontology_math_check_opening_20260916.md`. If the target Donut/Whirligig passages are not inside the first 60 user messages, advance using the extractor's `next_user_create_time` cursor in another bounded request. Once both historical operator specimens are captured verbatim, freeze their exact message IDs/timestamps/authorship and only then formalize the representation-invariance comparison.

## Handoffs / Nathan action
No Sable workflow redesign needed. No Nathan action required.
