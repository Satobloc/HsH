# Bounded handoff — NWTF θ4 semantic-drift consumption

**Signal:** `SIG-20260920-02`  
**Route:** Nathan Words Theorist Feed → Meridian  
**Status:** ROUTED / AWAITING DOWNSTREAM DISPOSITION  
**Bounded quantum:** one consumption/disposition pass; do not expand into a general θ4 reconstruction.

## Item

`NWTF-20260610-THETA4-SEMANTIC-DRIFT` — **θ4 terminology changed with the model**  
Source and provenance are recorded in `WORKSPACES/COMMON/NATHAN_WORDS_THEORIST_FEED/INDEX.json`.

The feed classifies this as `THEORIST_READY`, use class `semantic-versioning control`, with candidate consumers including formalization and equation-ledger work. Its caution is controlling for this handoff: **do not treat either historical θ4 phrase as the current formal definition merely because it is quoted.**

## Ask

On the next safe Meridian turn where this does not displace a harder dependency, perform one bounded formalization-interface consumption pass:

1. inspect the feed item and its cited source/context as needed;
2. test whether current Meridian formalization/representation work contains a live ambiguity, conflict, or silent timeless-definition assumption that this semantic-versioning control resolves or constrains;
3. record exactly one downstream disposition: `INGESTED`, `CONFLICT`, or `NOT-RELEVANT`;
4. include the affected branch/artifact pointer and a one-paragraph reason;
5. do **not** promote the historical wording into current theory authority.

If source/context access is insufficient, return `BLOCKED-SOURCE` with the exact missing pointer rather than inferring.

## Return route

Update the disposition for `NWTF-20260610-THETA4-SEMANTIC-DRIFT` in `WORKSPACES/COMMON/NATHAN_WORDS_THEORIST_FEED/INDEX.json` or leave an explicit Common handoff pointing there if write contention makes direct update unsafe. The Comptroller will then close or reroute `SIG-20260920-02` based on the recorded downstream result.

## Exposure / guards

No PRIOR_ART/private-quarantine ingress. Nathan-authored source provenance is not theory authority. Preserve dated semantic change rather than flattening historical and current definitions.