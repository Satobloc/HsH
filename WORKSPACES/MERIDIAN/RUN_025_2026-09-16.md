# Meridian run — 2026-09-16 16:25 EDT

## Scope
Source-first Whirligig/Donut reconstruction. No quarantine, PRIOR_ART, nLab, or external-literature exposure. No conversation-title action.

## Startup/control coverage
Read `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, `CURRENT_HSH_HYPOTHESIS_STATUS_2026-09-14.md`, `COORDINATION.md`, `HANDOFFS.md`, `WORKSPACES/SABLE/README.md`, and `WORKSPACES/MERIDIAN/TRIAL_CHECKPOINT.md`.

## Primary object + operation
Object: the March 13 `Ontology and Math Check` source identified by the generated chronology as `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_3/26.03.13•26.04.21•Ontology and Math Check — raw.json` (813 messages).

Operation: establish whether the proposed full-Donut-vs-reduced-Whirligig representation benchmark is ready for executable reconstruction from source.

## Result
The chronology resolves the exact raw source path and date window, but the GitHub connector cannot return the body of this large one-line raw JSON through `fetch_file` (metadata/empty content only), and direct raw fetch is rejected as too large/unsupported. Therefore I am **not** reconstructing the full Donut operator from remembered/excerpted definitions or silently filling missing equations.

This freezes a useful benchmark gate:

1. **Historical full operator specimen:** must be extracted verbatim from the March 13 source around the sequence already identified in prior source work: two closed curves / tangent transport and crossover; Great Annulum; chord and midpoint; `W_obble`; perpendicular `W_iggle`; rotational vector `V`; Donut with `P_ivot`, `Donomphulon`, `BigRad`; synchronized circuits; `Curvy_Lisa`; intended `Q wrt R` output.
2. **Historical reduced specimen:** the later named Whirligig two-input sphere→torus map must likewise be captured verbatim, including its exact input types, angle/phase definitions, torus parameterization, and any constraints.
3. **Only after both specimens are source-frozen** should the representation comparison be run. The test question is not visual similarity but whether there exists a stated map/quotient under which both constructions preserve the same relation/output for the same admissible inputs.
4. Required comparison observables should include at minimum: input-domain correspondence; winding/phase relation; tangent/crossover behavior; dependence on anchor/frame choices; output trace equivalence up to explicitly allowed reparameterization/rigid motion; and failure cases where the reduced map collapses distinctions retained by the full mechanism.
5. A failure to establish equivalence is a valid solver-library result; the reduced formula should then be typed as a surrogate/descendant rather than an equivalent representation.

## Infrastructure finding
The generated chronology remains a reliable path resolver for this source, but the current GitHub connector is not a sufficient large-raw-body reader for this 813-message one-line JSON. The project already documents a bounded raw-window extraction workflow (`.github/workflows/extract-raw-window.yml` + `WORKSPACES/COMMON/scripts/extract_raw_window.py`) for exactly this class of problem. A future bite should use that bounded extraction route rather than broad search or memory reconstruction, after locating the relevant message/time anchors without entering quarantine.

## Sandbox/theory status
No theory construction performed. This run defines a source/reproducibility gate for a future sandbox benchmark; no solver equivalence is asserted.

## Failure / uncertainty
Could not directly read the target raw body through the available connector path. Exact message IDs/timestamps for the full and reduced specimens are therefore not frozen in this run.

## Current frontier / next operation
Use the bounded raw-window extractor or another deterministic large-file reader to capture the two March 13 operator specimens verbatim with message IDs/timestamps and authorship. Then formalize only what those passages actually specify and run the full-vs-reduced representation benchmark.

## Nathan action
None. This is routable infrastructure/source-access debt, not a Nathan dependency.
