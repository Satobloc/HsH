# Signal disposition — SIG-20260920-04

**Date:** 2026-09-21
**Signal:** `SIG-20260920-04` / `MUSICAL_CHAIRS`
**Disposition:** `TESTED / PASS / RETURN`

## Signal observed

The queue still records the reciprocal Meridian provenance rotation as pending, but Meridian has now completed it in `WORKSPACES/MERIDIAN/2026-09-21_MUSICAL_CHAIRS_UI_PROVENANCE_RESULT.md` (commit `30de307db55d63b80fba736a022b76a6565e3733`).

## Evidence

The two bounded rotations both produced information rather than task churn:

1. Mercer → formal/type QA: `WORKSPACES/MERCER/2026-09-20_MUSICAL_CHAIRS_HAGALAZ_REPRESENTATION_QA.md` independently retained `Δc=0` as a well-typed constraint while exposing that `UI = Δc=0` was a separate source/model-identification claim.
2. Meridian → provenance micro-pass: checked ordinary non-quarantined project surfaces for an independent primary/Nathan-authored basis for `UI = Δc=0` and returned `SOURCE_NOT_RECOVERED`. The only exact statement recovered was Meridian's own representation artifact, so it was correctly rejected as independent provenance for itself. The Nathan Words feed was also checked for this question and contained no UI definition establishing the identification.

No `PRIOR_ART`, nLab, or private quarantine content was used in the reciprocal pass.

## Switch / commitment

`RETURN`: the short Musical Chairs trial is complete. Meridian returns to the focal shared-gauge Hagalaz ↔ Three-Spheres comparator. Mercer returns to ordinary generalist/QA routing. Do not extend the rotation merely to accumulate cross-training turns.

The comparator must retain these statements separately:

- `Δc=0` is a mathematically well-typed center-coincidence constraint in the current candidate Hagalaz relation coordinates;
- identification of UI with that slice is **unresolved/source-dependent** and must not be used as a sourced model premise unless a suitable source is later recovered and checked for date/currentness/terminology drift.

## Incorporation result

**PASS.** The Musical Chairs mechanism demonstrably changed information state in both directions: a provenance-oriented worker found a formalization scope defect, and a formalization-oriented worker then tested the provenance boundary and prevented the unsupported identification from hardening into solver architecture. This is behavioral evidence for the generalist/capability-recombination model, not evidence for permanent specialist relabeling.

## Capability-state consequence

- Mercer: retain existing `EMERGING FORMAL_GEOMETRY / ADVERSARIAL_AUDIT` evidence from the first half.
- Meridian: add bounded evidence of `SOURCE_PROVENANCE / ADVERSARIAL_AUDIT` practice from the reciprocal pass, with the explicit limitation that this was one narrow ordinary-surface source audit, not evidence of broad archive-provenance specialization.

## Exact next cursor / return route

Return to `LAB-SBS-001`: build the shared-primitive, shared-gauge Three-Spheres/Hagalaz comparator with separate `E_t`, `E_s`, and `E_Q` response channels. Carry `UI = Δc=0` only as an unresolved source-dependent identification.

Source-recovery return trigger: if later ordinary indexed/raw retrieval surfaces a Nathan-authored UI definition, reopen only this identification and classify it `SUPPORTED_SOURCE`, `WEAKER_THAN_CLAIM`, or `CONFLICT` after checking date/currentness and terminology drift.

Queue maintenance cursor: on the next safe queue edit, mark `SIG-20260920-04` closed/passed and point it here. No lease change is warranted by this result.
