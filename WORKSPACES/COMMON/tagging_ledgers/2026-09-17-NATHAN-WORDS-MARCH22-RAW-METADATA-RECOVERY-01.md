# Nathan Direct — March 22 raw metadata recovery checkpoint

**Date:** 2026-09-17  
**Status:** bounded provenance checkpoint / negative infrastructure result

## Operation

Attempted the next cursor from the March 22 morning geometric-substrate pass: recover raw UUID / `author.role=user` metadata for the high-value Nathan turns in Raw#1–57 before making stronger identity claims.

## Sources checked

- current Common control/state surfaces and Nathan Direct workflow state;
- Library transcript `Pasted text(20260914-172536).txt`;
- duplicate Library transcript `Pasted text(20260912-054945).txt`;
- raw-conversation-derived copies surfaced inside `ALDEN CROSS — raw*.json`;
- durable `indexes/nathan-direct/nathan-direct-2024.jsonl` existence and current blob identity on `main`.

## Result

The readable transcript representations preserve Nathan/ChatGPT/bio labels, Raw# numbering, exact text, and timestamps. They expose the March 22 sequence cleanly, including Raw#1 at 11:10:05 AM and Raw#57 at 12:57:54 PM.

However, this bounded pass did **not** recover the underlying original conversation/message UUIDs or raw `author.role` objects for those historical Raw turns. Searches for the exact Raw#1/Raw#57 wording plus `message_id`, `conversation_id`, UUID, and role metadata returned transcript manifestations and later tool-response embeddings, not the original raw message objects.

The durable Nathan Direct 2024 shard is confirmed present on `main` as `indexes/nathan-direct/nathan-direct-2024.jsonl` (current blob SHA observed: `c5b18ad317f87d7838ad72e959e3403c9ff83e2d`), but the connector's bounded text fetch did not expose its JSONL records in this run. Therefore I am **not** inventing UUIDs, inferring raw role metadata from the transcript label alone, or claiming raw-message authentication beyond what has actually been recovered.

## Duplicate handling

`Pasted text(20260914-172536).txt` and `Pasted text(20260912-054945).txt` reproduce the same March 22 Raw-numbered sequence for the inspected anchors. They are treated as duplicate transcript manifestations, not independent testimony events. Likewise, copies embedded in later raw conversations are secondary retrieval manifestations.

## Provenance status retained

The March 22 transcript remains strong direct-attribution evidence because it explicitly labels the relevant turns `Nathan`, with exact wording and timestamps. The stronger metadata-authenticated status (`message UUID` + raw `author.role=user`) remains **unresolved for this source family in this lane** until a raw-export anchor is recovered.

This does not weaken the content-level result already recorded: the visible sequence is lower-dimensional line/helix/plane intersection geometry → moving-section apparent kinematics → generalized motion vocabulary → Raw#57 dimensional lift and adoption of `strings` / `particles`. It only narrows the metadata-authentication claim.

## Current frontier

Do not spend another blind search cycle on UUID recovery. Best next bounded operation is one of:

1. use the repo-native raw-window ancestry route only after a defensible raw conversation path/time anchor is located, consistent with Mercer's current guidance; or
2. proceed to the adequately scoped pre-2024 Nathan Direct search for predecessors of the moving-section/intersection construction, stating package/search coverage explicitly and treating any candidate as provisional until exact raw context is checked.

No Nathan action is required.
