# Mercer Run 51 — 2026-09-15

## Scope
Archive/index/retrieval/provenance/documentation QA. No theory construction. Quarantine not accessed.

## Sources / coverage
- `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`
- `WORKSPACES/COMMON/WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/COORDINATION.md`
- `WORKSPACES/COMMON/CROSS_REPO_SCRIPT_EXECUTION_STANDARD.md`
- `WORKSPACES/COMMON/SHARED_STATE_WRITE_SAFETY.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md` through Run 50
- GitHub Actions run `35016148194`, commit `326142169a01eb0f777508c27df0c8ffdf72a65c`, job `104539952381`
- `.github/workflows/mercer-cross-source-integrity.yml`

## Material result
The missing production-v2 gate has now been observed and is green. Workflow run `35016148194` completed `success`. The seven-specimen competence harness passed all seven specimens. The production v2 validator reported overall `BLOCKED` with **0 FAIL / 1 BLOCKED / 0 WARN / 0 UNKNOWN / 13 PASS**. The sole blocker is the already-known SAT_CONVOS_15 filename-normalization collision. All four replacement Viewer invariants passed:
- `viewer.accepted_input_reconstruction`
- `viewer.post_dedup_cardinality`
- `viewer.resolved_winner_reconciliation`
- `viewer.external_input_lineage`

The same run's dedup diagnostic independently reconstructed **454 accepted -> 453 post-dedup** and identified the sole duplicate path as `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/26.06.22•26.09.12•Cosmological Constant Summary — raw.json`, with manifest index 253 (`unchanged`) surviving index 257 (`collision`).

The legacy validator, run in the same checkout as a reference, still reported its known false `viewer.accepted_input_arithmetic` FAIL because it compares pre-dedup 454 directly with post-dedup 453. This is now retained as historical/reference behavior, not the production contract.

## Infrastructure change
Updated `.github/workflows/mercer-cross-source-integrity.yml` at commit `02b4ac3a8264fffd2d70afda7f5d4a2f2c8834ad` to remove transition wording: `validate_cross_source_integrity_v2.py` is now labeled the production metadata validation step; the old implementation remains explicitly labeled `Legacy validator reference`, `continue-on-error`.

This is a repository workflow-role change only. No conversation/thread/chat identity was changed or proposed for change.

## What the checks establish / do not establish
They establish that, for the current generated metadata and frozen competence specimens, the semantics-aware validator reconciles production resolved-input acceptance, exact-path dedup cardinality, winner provenance, and declared external lineage without integrity failures. They do not clear the filename-normalization collision, disposition the duplicate source artifact, validate raw conversation content, establish authorship, or imply theory correctness/currentness.

## Current frontier
Validator transition is complete at the contract/CI-role level. The SAT_CONVOS_15 duplicate remains independently `BLOCKED` pending provenance-preserving disposition. Other retained Mercer dependencies remain unchanged.

## Best next operations
1. Observe the workflow run triggered by the production-role label change only as a smoke check; do not manufacture further validator churn if it remains green.
2. Return primary attention to unresolved archive provenance/duplicate work, especially a provenance-preserving disposition path for the SAT_CONVOS_15 identical duplicate without conflating Viewer dedup with source cleanup.
3. Opportunistically inspect machine-readable autotag generation/freshness lineage if duplicate disposition is still owner-blocked.

No Nathan action required.
