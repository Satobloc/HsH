# Mercer Run 73 — relation-sidecar contract harness execution

**Date:** 2026-09-16
**Lane:** archive/index/retrieval/provenance/documentation QA
**Operation:** execute the Run-72 synthetic Viewer conversation-relation sidecar contract harness without touching production Viewer state.

## Startup / boundaries
Read the hard no-conversation-renaming policy, worker autonomy/handoff protocol, automation workflow control, current coordination and handoffs, Mercer checkpoint, cross-repo script execution standard, and shared-state write safety. No quarantine material was read. No theory work was performed. No conversation/thread/chat identity or title was changed.

## Exact source / coverage
- `WORKSPACES/MERCER/test_conversation_relation_sidecar_contract.py`, current blob SHA `0c5e81fdf1299eb619a87416aaa9c1202c8e32b8`.
- Harness is synthetic and production-isolated: no raw conversation exports, Viewer production catalog, quarantine material, or archive source content are inputs.
- Seven relation classes exercised: exact-byte; message-id-subset; message-id-superset; same-graph-metadata-only; same-graph-readable-text-divergent; same-graph-nontext-unresolved; branch-snapshot-divergent.
- Negative fixtures exercised: stale source commit, Viewer-ID/source-path mismatch, unsupported inferred class (`canonical`), relation endpoint outside family, and direction on a non-subset/superset relation.

## Execution result
Executed the fetched Run-72 harness logic in an isolated Python runtime. Result:

`PASS: sidecar contract synthetic fixtures`

All assertions completed without exception. This establishes that the staged synthetic contract logic accepts its seven-class valid fixture and rejects each of the five encoded invalid cases under the current implementation.

It does **not** establish that a production sidecar generator exists, that production Viewer/catalog joins are correct, that every malformed sidecar is rejected, or that the Run-71 interface architecture has Sable approval. It also does not confer ancestry authority/currentness/disposition on any source.

## Artifacts changed
- Added this run record only.
- No production Viewer/catalog/generated relation state changed.
- No archive source changed.

## Infrastructure / capability change
The Run-72 harness can now be treated as runtime-green for its encoded synthetic fixtures rather than merely staged. The next infrastructure step should remain gated on Sable/interface-owner review; Mercer should not unilaterally implement the shared Viewer schema.

## Frontier / blockers / handoffs
- Run-71 Viewer relation sidecar architecture remains pending Sable/interface-owner review.
- The synthetic harness is green but is not yet wired into CI; whether it belongs in the production integrity workflow is a shared workflow-design decision rather than a Mercer-local assumption.
- Existing SAT_CONVOS_15 duplicate-disposition/normalization blocker remains unchanged.

## Best next operation
Inspect current Sable response/state for the Run-71 Viewer relation-sidecar proposal. If no decision exists, choose another bounded Mercer QA target rather than implementing the shared schema speculatively.
