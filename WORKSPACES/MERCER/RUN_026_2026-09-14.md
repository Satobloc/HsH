# Mercer Run 26 — 2026-09-14

## Scope

Bounded retrieval/index-integrity and owning-documentation QA under the active theory standdown. No theory-bearing synthesis performed.

## Startup/control surfaces read

- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`

No newer Nathan directive altered the Mercer lane. `MORROW-SOURCE-001` remains open but no new committed historical candidate-report owner/path surfaced in this run.

## Exact source regions inspected

- `CONVERSATION_VIEWER/README.md` — catalog-build and provenance sections.
- `CONVERSATION_VIEWER/data/` directory listing — verified current durable data surfaces: `annotations.json`, `conversations.json`, `landmarks.json`.
- `CONVERSATION_VIEWER/CURATION.json` — current schema version 1 with empty `rules` list.
- `tools/build_conversation_viewer.py` — defaults, curation-copy behavior, manifest construction, and `main()` output path.

## Finding

The owning Viewer README said the build command “generates only” `CONVERSATION_VIEWER/data/conversations.json`. That statement is too strong for the current builder. The implementation always treats the catalog as the primary output, but when partial curation rules exist it can additionally create derived viewer-only JSON copies under `CONVERSATION_VIEWER/data/curated/`; whole-conversation hiding also alters the derived catalog without rewriting source conversations.

The discrepancy is presently latent rather than active because `CONVERSATION_VIEWER/CURATION.json` contains no rules. It is still a documentation-integrity problem: a future valid use of partial curation would make the README's invariant false.

## Change made

Updated `CONVERSATION_VIEWER/README.md` so the build section now states:

- the normal catalog output is `CONVERSATION_VIEWER/data/conversations.json`;
- raw sources continue to be fetched from canonical paths;
- partial curation may create derived copies under `CONVERSATION_VIEWER/data/curated/`;
- those copies preserve source coordinates and do not modify/replace raw archive conversations;
- whole-conversation hiding affects only the derived catalog.

Commit: `57f85c1f71ca3407a80dd665a72f279a0e56e1f7`.

## Epistemic / ownership boundary

This was documentation/navigation reconciliation only. No generated Viewer catalog or raw conversation was hand-edited. No curation rule was introduced. No theory status, authorship, chronology, or archive-content interpretation was changed.

## Current frontier

This closes one concrete Viewer owning-documentation mismatch. The next Mercer operation should remain bounded and measurable: inspect another owner-facing index/retrieval invariant against its implementation or source surfaces, preferably where stale documentation could misroute future maintenance. Do not reopen broad glossary ancestry scans, do not poll terminal navigation run `34814002515`, and do not duplicate Morrow conversation-family continuity or the Nathan-words tagging lane.

## Blockers / dependencies

- Direct raw-message ancestry for historical glossary/crosswalk artifacts remains unresolved and deliberately closed pending a stronger anchor.
- Fresh canonical Viewer/generated-state regeneration remains an owner-execution dependency.
- Historical/manual scanner candidate-report owner/path remains unidentified.
- Exact raw IDs for September 13 Mercer live-source statements remain pending an attributable export/provenance surface.

## Commons / Nathan attention

No Common handoff was warranted: the correction was made directly at the owning Viewer documentation surface and creates no new cross-lane dependency. No Nathan decision is required.
