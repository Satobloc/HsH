# Mercer Run 109 — 2026-09-18

## Object / operation
Inspected one non-LAB1 retained positive-context extraction as a bounded negative-control sample for the graph/chronology investigation.

## Exact sources / coverage
- `WORKSPACES/COMMON/extraction_outputs/meridian_ontology_math_check_opening_20260916.json` — request metadata plus visible retained opening sequence through the sampled continuation.
- Current Common no-conversation-renaming, worker-autonomy, automation-control, coordination/handoffs, execution-safety/shared-state-safety surfaces, and Mercer checkpoint were read before the operation.

## Finding
The extraction request explicitly sets `context_each_side: 1`, so the Run-108 omitted-field classifier-default defect does not affect this retained request.

Within the inspected visible retained opening sequence, every successor after the first visible row records the immediately preceding visible node as its `parent`: `f8645e42… -> f89fa3ac… -> 32ed0024… -> 0a125d48… -> 3e7c20d5… -> 8fc3908a… -> 454f30c2… -> b41999f7… -> 4fafb49e… -> 8928b9bc… -> 493b8833… -> b9304a40…`. Thus the inspected prefix is graph-local in chronological order.

The first visible user row records parent `3bb66876…`, which is outside the inspected retained prefix. That boundary condition is not by itself evidence of a cross-branch splice: the extractor's retained window may begin after an omitted/nonselected ancestor. Classification should therefore distinguish boundary-missing ancestry from demonstrated incompatible adjacency inside the retained sequence.

## What this establishes / does not establish
This supplies a non-LAB1 graph-local negative control and reinforces that retained chronological windows are mixed: some inspected windows are graph-local while Runs 99–104 document divergence elsewhere. It does not classify the entire Meridian extraction, establish corpus prevalence, or change the earlier cross-family divergence findings.

## Archive infrastructure / enrichment
Classifier interpretation rule sharpened: do not infer a splice from a missing ancestor outside a retained-window boundary; classify only adjacency actually represented/testable in the reconstructed window, with boundary/unresolved status kept separate.

## Current frontier / blocker
Corpus-wide deterministic prevalence remains blocked in this connector route because the large declared raw conversation JSON sources cannot be materialized/read into an executable exact checkout. The repaired classifier remains ready for execution when that route exists.

## Next operation
If classifier execution remains unavailable, inspect one fresh archive/retrieval QA object rather than accumulating additional extraction anecdotes. Revisit extraction prevalence only when exact readable raw-source execution becomes possible.

No theory work performed. No quarantine material accessed. No conversation/thread/chat identity altered. No Nathan action required.
