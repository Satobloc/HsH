# Mercer Run 103 — 2026-09-18

## Scope
One-object read-only extraction-context QA bite. Read current hard UX policy, worker autonomy/control surfaces, coordination/handoffs, and Mercer checkpoint. Primary target: `WORKSPACES/COMMON/extraction_outputs/2026-09-14-RUSSIA-AFTER-015706.json`, blob `1872020a4036a1b4059723eb41386c1a6ac76f71`.

## Check performed
Inspected the beginning of the retained extraction and compared chronological row adjacency against preserved `node_id` / `parent` graph edges. Request uses `context_each_side: 2`.

## Result
Positive branch-splice reproduction outside LAB1.

The first retained assistant row `cd498379-9c2d-4e34-976d-9588424a4b8f` records parent `d22eb263-0b5d-482e-8a16-b4c45f927c30`, but that parent user row appears immediately *after* it in the timestamp-sorted retained sequence. More importantly for whole-segment continuity, the later selected/anchor-region user row `9623217b-5bad-40f5-86b2-0f00c7502f01` records parent `7d999c16-2156-473a-a377-f97c20fbbf4c`, not the chronologically preceding retained user `d22eb263-0b5d-482e-8a16-b4c45f927c30`. Its following assistant `3efc8949-4578-4e39-ae1e-758c81bfa9d5` correctly records `9623217b-...` as parent.

Thus chronological extraction context is demonstrably not a single continuous parent/child path in this retained RUSSIA output. This extends Runs 99–101 beyond LAB1 and uses a larger positive context radius (`2`). It remains an extraction-context provenance/semantics finding, not source corruption and not a measure of downstream interpretive impact.

## What this establishes / does not establish
Establishes: branch/chronology divergence occurs in at least one retained non-LAB1 positive-context extraction; the issue is not confined to one conversation family or `context_each_side: 1` samples.

Does not establish: corpus-wide prevalence; whether every selected target in this file is affected; whether the chronologically adjacent material changed any downstream interpretation; any theory correctness claim.

## Artifacts changed
This run record only, plus Mercer checkpoint continuity update. No extraction output/source, Viewer/generated state, theory state, quarantine material, or conversation identity changed.

## Enrichment
Audit criterion strengthened: for timestamp-flattened extraction windows, graph-locality must be evaluated as a path/segment property and should tolerate timestamp inversions only as a separately classified condition rather than assuming row order encodes ancestry.

## Frontier / next operation
Continue one-object deterministic sampling/classification on another retained positive-context extraction, preferably a different conversation family. Longer-term validator should report at least: immediate graph-local, same-branch/non-immediate, cross-branch/spliced, timestamp-order inversion, unresolved.

## Handoff / workflow observation
Existing extraction/Sable routing remains appropriate. This cross-family reproduction raises the value of a deterministic read-only validator but does not require Nathan action or a cross-lane redesign by Mercer.