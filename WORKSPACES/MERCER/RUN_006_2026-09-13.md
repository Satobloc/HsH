# Mercer Trial Run 6 — 2026-09-13

**Status:** bounded archive/provenance QA under active project-wide theory standdown.

## Startup checks

Read:
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`
- `WORKSPACES/MERCER/RUN_005_2026-09-13.md`
- `WORKSPACES/COMMON/scripts/find_superset_conversation_duplicates.py`
- `WORKSPACES/MERCER/SUPERSET_SCANNER_QA_2026-09-13.md`
- repository `.github/workflows/` listing
- `.github/workflows/maintain-navigation.yml`
- repository `tests/` listing

Standdown remains active. `MORROW-SOURCE-001` remains the highest-priority bounded source-integrity issue explicitly handed to Mercer.

## Work completed

1. Traced the scanner beyond code search by directly listing `.github/workflows/`; no committed workflow invoking `find_superset_conversation_duplicates.py` was found.
2. Confirmed `maintain-navigation.yml` runs `python -m unittest discover -s tests -v`, giving an existing durable regression-test surface.
3. Added `tests/test_superset_conversation_duplicates.py` in commit `f2bc84d5ea9632bd019ef442b4f6ff30b921e146`.
4. The committed tests cover:
   - full `execution_output.text` versus an interior `...[truncated]...` replacement: hashes must differ;
   - channel-only difference: hashes must differ;
   - JSON object-key reordering only: hashes must remain equal.
5. Queried workflow runs associated with the test commit; none were returned at that time, so CI success is not claimed.
6. Updated `WORKSPACES/MERCER/SUPERSET_SCANNER_QA_2026-09-13.md` in commit `2a4ef4395b25192ea91a2f38318fedd548ef449e` to record committed regression coverage and the remaining output-side dependency.

## Issue status

`MORROW-SOURCE-001` is now **code-side resolved / output-side pending**:
- scanner equality bug fixed;
- conservative complete-content payload policy documented;
- exact Janus regression durably recorded;
- executable regression test committed into the repository's existing unittest discovery path;
- CI result not yet observed;
- old generated candidate reports remain stale, but no committed scanner invocation/report surface is discoverable to regenerate.

Preserve the earlier Janus export. Do not disposition it from pre-patch scanner output.

## Current frontier

Remain inside the standdown. On the next run, first recheck Control/Common and CI/ownership state. If the regression tests have run successfully, the code-side dependency can be treated as fully closed; only identification/regeneration of any old external/manual candidate report remains. If a newer Common handoff supersedes this, take the newer eligible source/provenance task.

## Blockers / dependencies

- `DEPENDENCY`: no committed invocation/output surface exists for the scanner, so stale historical generated reports cannot be located or regenerated from repository state alone.
- `DEPENDENCY`: CI result for the new regression commit was not yet visible.

Neither requires Nathan attention.

## Nathan attention

Not required.
