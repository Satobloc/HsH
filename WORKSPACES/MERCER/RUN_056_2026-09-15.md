# Mercer Run 056 — 2026-09-15

## Scope
Convert Runs 54–55's one-off repeated-`conversation_id` finding into a reproducible, corpus-wide, read-only identity diagnostic before considering any Viewer identity policy.

## Startup / controls read
Read current `NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, `COORDINATION.md`, `HANDOFFS.md`, `CHECKINS.md`, `TRIAL_CHECKPOINT.md`, `CROSS_REPO_SCRIPT_EXECUTION_STANDARD.md`, and `SHARED_STATE_WRITE_SAFETY.md`. No conversation/thread/chat identity was renamed, retitled, altered, or proposed for renaming. No quarantine material was accessed.

## Exact source / coverage
- `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/4D Topological Model Assessment — raw.json` — direct repository source; blob `3ae831d52775f6a946a669836ee344b3580eb33d`, 361104 bytes.
- `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/4D Topological Model Assessment — raw (1).json` — direct repository source; blob `7bc6ff0c782a797efcf38ff2e3a440db221ca22c`, 361104 bytes.
- Both direct headers again expose raw conversation UUID `69f9ee83-bac4-8330-8460-0ae605478757`; visible top-level `safe_urls` order differs.
- Viewer-resolved accepted input semantics supplied by `WORKSPACES/MERCER/viewer_input_semantics.py`.

## Work completed
Created `WORKSPACES/MERCER/diagnose_conversation_identity_duplicates.py`, a read-only diagnostic that scans the Viewer-resolved accepted JSON source set, groups raw exports by `conversation_id`, and reports pairwise classifications without suppressing or modifying sources.

The diagnostic records separate hashes for raw bytes, canonical JSON, and the raw `mapping` message graph; counts message IDs/shared IDs; reports differing top-level keys; and distinguishes conservative classes: exact bytes, JSON-semantic exact, top-level order-only, same message graph with top-level metadata differences, message-ID subset/superset candidates, same-ID-set divergent payload, and unresolved/divergent branch.

Updated `.github/workflows/mercer-cross-source-integrity.yml` so this diagnostic is part of the retained Mercer QA artifact set and changes to the diagnostic trigger CI. The diagnostic writes only its CI report under `/tmp/mercer-integrity/`; it does not alter raw archive sources or Viewer data.

Implementation commits: initial diagnostic `2cbe76de677965cd2791426d82e0ec8fd2fd8b03`; corrected adapter integration `bd78940c12b8de6031da4cb64515475958fbb91c`; CI integration `309b42f72ce4ddb94ef2ac0e5b1494fe7c6a583c`.

## Checks / current execution state
GitHub Actions run `35049604577` was triggered on commit `309b42f...`. At the last observation in this run, the job was `in_progress`; the new `Conversation identity duplicate diagnostic` step had not yet executed because checkout was still running. Therefore this run establishes the reusable diagnostic and CI wiring, **not yet the corpus-wide classification result**. Do not infer semantic equality of the Run-54 pair until the retained diagnostic output is observed.

## Archive-infrastructure change
Identity QA now has an explicit three-layer model in executable form: raw artifact/path identity, raw ChatGPT `conversation_id`, and Viewer presentation identity. The new diagnostic reports collisions at the middle layer while preserving the outer layers and source ancestry.

## Theory / enrichment
No theory work performed. Capability gain: repeated-conversation identity can now be audited reproducibly over the production-resolved Viewer input set rather than by title/path heuristics or isolated manual inspection.

## Frontier / dependencies
1. Observe run `35049604577`; inspect retained `conversation-identity-duplicates.json` and classify every repeated UUID family found.
2. Verify specifically whether the `4D Topological Model Assessment` pair is order-only, metadata-only, prefix/superset, or divergent.
3. Only after corpus-wide classification consider a reporting invariant in the production validator. Do not make suppression/disposition policy from UUID equality alone.
4. Keep the separate `Cosmological Constant Summary` exact-path/normalization collision unresolved until owner disposition.

## Workflow observation / handoff
No cross-lane redesign proposed. If the corpus scan reveals a nontrivial family taxonomy requiring Viewer behavior changes, route the policy question to Sable/workflow ownership with the retained diagnostic evidence. No Nathan action required at this stage.