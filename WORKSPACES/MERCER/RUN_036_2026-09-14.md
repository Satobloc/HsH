# Mercer Run 36 — 2026-09-14

## Control / scope
Read current `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, `COORDINATION.md`, `HANDOFFS.md`, and `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`. Newer Nathan directive supersedes the old blanket training standdown: direct theory work is sandbox-limited; quarantine remains off-limits. This run stayed entirely in archive/integrity QA.

## Operation chosen
Closed the explicit Run-35 frontier: inspect workflow run `34918942881`; if failed, repair the competence harness without weakening classifications.

## Exact result
GitHub Actions run `34918942881` completed **failure**. Job `104222530595` shows:
- checkout: success;
- regression fixture: success;
- failure-mode competence harness: failure;
- production metadata validation: skipped;
- report upload: success.

Inspection of `WORKSPACES/MERCER/test_integrity_failure_modes.py` against `validate_cross_source_integrity.py` found the harness was calling four stale/nonexistent check IDs. The validator's current IDs are:
- `viewer.input_freshness.development`;
- `nathan_direct.user_record_arithmetic`;
- `nathan_direct.stage2_source_continuity`;
- `manifest.operational_blockers.indexes/manifests/development-conversation-dates.json`.

The harness instead expected older guessed labels for those checks. This is a test-interface drift defect, not evidence that the validator's classifications are wrong.

## Change
Updated only `WORKSPACES/MERCER/test_integrity_failure_modes.py` to use the validator's actual check IDs. Expected specimen classifications were **not weakened or changed**: clean PASS; stale Viewer WARN; catalog-count FAIL; Nathan Direct arithmetic FAIL; Stage-2 continuity FAIL; collision BLOCKED with no FAIL.

Commit: `fd948c412436a295eebaf78689b7e49325a8dc82`.

The push triggered Mercer integrity workflow run `34926821342`; it was queued when this run ended. Navigation maintenance also queued independently from the same push. Do not claim the repaired harness passes until the workflow completes.

## What this establishes / does not establish
Establishes the concrete cause of Run-35 CI failure and repairs the harness/validator naming interface. It does not yet establish a green CI result for the repaired commit, and it does not alter or validate theory content.

## Artifacts changed
- `WORKSPACES/MERCER/test_integrity_failure_modes.py`
- `WORKSPACES/MERCER/RUN_036_2026-09-14.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md` (continuity update)

No raw conversation, generated catalog/manifest, tag, curation rule, or theory-bearing source was edited.

## Frontier / next operation
Inspect terminal result of Mercer workflow run `34926821342`. If green, freeze the six specimens as initial competence/regression set and return attention to source-integrity production state (especially any fresh navigation regeneration/collision change). If it fails, inspect the failing step and repair only the demonstrated interface/logic defect without weakening expected classifications.

## Blockers / handoffs
Known SAT_CONVOS_15 duplicate-disposition dependency remains owner-routed. No Nathan action required from this run.
