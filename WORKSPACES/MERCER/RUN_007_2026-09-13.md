# Mercer Trial Run 7 — 2026-09-13

**Status:** bounded archive/provenance QA under active project-wide theory standdown.

## Startup checks

Read current:
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`
- `WORKSPACES/MERCER/RUN_006_2026-09-13.md`
- `WORKSPACES/MERCER/SUPERSET_SCANNER_QA_2026-09-13.md`
- `WORKSPACES/COMMON/scripts/find_superset_conversation_duplicates.py`
- regression commit `f2bc84d5ea9632bd019ef442b4f6ff30b921e146`

The theory-bearing standdown remains active. `MORROW-SOURCE-001` is still the highest-priority bounded source-integrity issue explicitly assigned to Mercer.

## Work completed

1. Re-read the current scanner from `main` and confirmed the conservative equality payload remains author role/name + entire `content` object + recipient + channel + create_time.
2. Checked GitHub combined status for the regression-test commit. No status contexts were present; CI success is therefore still not claimed.
3. Independently executed all three committed regression conditions against the current `canonical_message()` logic:
   - `execution_output.text` full body versus interior truncation marker => different hashes: PASS;
   - channel-only change => different hashes: PASS;
   - JSON object-key reordering only => same hash: PASS.
4. Updated `WORKSPACES/MERCER/SUPERSET_SCANNER_QA_2026-09-13.md` to record this complete direct validation and sharpen issue state.

## Issue state

`MORROW-SOURCE-001` is **code-side resolved / output-side pending**.

Code-side:
- scanner bug fixed;
- full-content/channel policy documented;
- exact regression suite committed;
- all three regression conditions directly executed successfully against current logic.

Output-side:
- any pre-patch generated duplicate-candidate report remains stale;
- no committed scanner invocation/report surface has been found, so the owner/path needed for regeneration is still unknown.

Preserve the earlier Janus export. No duplicate disposition should rely on pre-patch output.

## Current frontier

Remain inside the standdown. The scanner itself no longer needs further Mercer work unless a failing CI result appears. Next useful action is to identify the provenance/owner of any historical candidate report or take a newer eligible source-integrity/documentation handoff from Common. Ordinary autotag QA, Viewer/catalog reconciliation, and theory-bearing methodology synthesis remain deferred until release.

## Blockers / dependencies

- `DEPENDENCY`: historical/manual scanner output owner/path remains unknown.
- No Nathan-required decision.

## Best next operation

At next run, reread Control/Common. If no newer handoff supersedes this issue, check whether Morrow or another owner has identified the stale report-generation path. If not, avoid repetitive scanner revalidation and branch to the highest-value permitted provenance/documentation maintenance task.
