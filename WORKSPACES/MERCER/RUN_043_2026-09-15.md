# Mercer Run 43 — 2026-09-15

## Scope / controls read

Read `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, `COORDINATION.md`, `HANDOFFS.md`, `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`, `CROSS_REPO_SCRIPT_EXECUTION_STANDARD.md`, and `SHARED_STATE_WRITE_SAFETY.md`. No conversation/thread/chat title was changed or proposed for change. Quarantine was not entered. No theory-bearing work was performed.

## Operation

Followed the Run-42 next operation: inspect the integrity workflow triggered by `c30dfcd8d52c1189d3b7ea3cddaf0f5051ded975` and retrieve the now-retained `viewer-input-dedup.json` diagnostic before changing the production validator.

## Exact result

GitHub Actions run `34960203633` completed **success**. Artifact `10392707034` (`mercer-cross-source-integrity`, digest `sha256:6d1226df87aaa57836e41227df6c263a78b463a9fee3c692751f1eed378b16dd`) retained `viewer-input-dedup.json`, `report.json`, and `report.md`.

The diagnostic result is:

```json
{
  "duplicate_paths": [],
  "reconstructed_accepted_total": 453,
  "reconstructed_post_dedup_total": 453,
  "viewer_declared_accepted_total": 454,
  "viewer_source_conversations_before_curation": 453
}
```

This falsifies the working Run-39/40 hypothesis that the observed `454 -> 453` arithmetic necessarily represents one exact-path deduplication. Under the current manifest/external inputs and the builder's acceptance rules, the reconstruction finds **no duplicate path at all**. The discrepancy occurs one stage earlier: the Viewer input metadata declares 454 accepted inputs, while the same declared source files reconstruct to 453 accepted records.

The arithmetic identifies the one-record location without requiring raw-conversation access. The development manifest summary is `364 unchanged + 74 planned + 121 skipped + 1 collision = 560`; builder/diagnostic acceptance excludes skipped/collision/blocked and nonpositive records, so development contributes `364 + 74 = 438`. Live contributes 9 and discovered external contributes 6, giving `438 + 9 + 6 = 453`. Yet the Viewer input metadata currently declares `accepted_json_conversations: 439` for development. The Viewer catalog's observed `development: 439` is not contradictory because one discovered external record itself declares corpus `development`; catalog corpus counts and source-input acceptance counts are different dimensions.

Production validator output in the same artifact remains `FAIL` only at `viewer.accepted_input_arithmetic`, with the known development-manifest collision separately classified `BLOCKED`; the other 11 checks pass. Nathan Direct state in this run has advanced to 15,133 packaged unique user messages + 7,625 collapsed duplicates = 22,758 input user records; shard sum and Stage-2 source continuity both reconcile at 15,133.

## Interpretation / limits

This establishes a metadata lineage inconsistency, not source corruption and not theory error. It does **not** yet establish why `CONVERSATION_VIEWER/data/conversations.json` carries development acceptance `439` while its referenced manifest reconstructs to `438`. A plausible historical-generator/version or publication-transaction explanation must not be promoted without commit-level reconstruction.

The current `tools/build_conversation_viewer.py` and `diagnose_viewer_input_dedup.py` agree on the relevant status exclusions (`skipped`, `collision`, `blocked`). Therefore the next regression specimen should encode the actually observed condition: Viewer-declared per-input acceptance exceeds source-reconstructed acceptance by one while post-reconstruction catalog cardinality is internally consistent and there are no duplicate paths. Do not encode a synthetic path-collision specimen as if it were the observed case.

## Artifacts changed

- Created this run record only.
- `TRIAL_CHECKPOINT.md` updated separately through Run 43.
- No generated Viewer/catalog/manifest, raw conversation, tag, curation rule, source artifact, or theory-bearing surface modified.

## Current frontier / best next operation

1. Add specimen seven reproducing the observed **declared-acceptance drift with zero path duplicates**.
2. Reconstruct the Viewer generation commit/history sufficiently to determine whether the `439` development input count arose from an older builder rule, stale/generated publish transaction, or another deterministic source.
3. Repair `validate_cross_source_integrity.py` only after that provenance is established. The repair should distinguish source-input acceptance reconciliation, post-dedup cardinality, and catalog corpus counts rather than treating them as one arithmetic invariant.
4. Keep the SAT_CONVOS_15 collision as a separate `BLOCKED` operational dependency.

No Nathan action is required.
