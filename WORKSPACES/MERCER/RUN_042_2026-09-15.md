# Mercer Run 42 — 2026-09-15

## Scope / controlling surfaces

Read and obeyed before work:
- `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`
- `WORKSPACES/COMMON/WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- current `COORDINATION.md` and `HANDOFFS.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`
- before workflow write: `WORKSPACES/COMMON/CROSS_REPO_SCRIPT_EXECUTION_STANDARD.md` and `WORKSPACES/COMMON/SHARED_STATE_WRITE_SAFETY.md`

No conversation/thread/chat title was changed or proposed for change. No quarantine material was accessed.

## Bounded operation

Followed up Run 41 Actions run `34954998631` rather than guessing the 454→453 duplicate.

The run completed **failure**. The ordinary regression fixture and frozen six-specimen competence harness both passed. The new `Viewer input dedup diagnostic` step failed; production metadata validation was therefore skipped. The run produced no artifact because `/tmp/mercer-integrity` was only created in the later production-validation step, which never ran.

This is an observability defect in the CI harness: the diagnostic prints its full JSON report before returning nonzero, but the workflow did not persist that output when the diagnostic itself was the failing step.

## Source-code recheck

Re-read `tools/build_conversation_viewer.py` against `WORKSPACES/MERCER/diagnose_viewer_input_dedup.py`.

The diagnostic still mirrors the builder's relevant metadata semantics at the code level:
- manifest canonical path = `new_path` else `old_path`;
- positive integer `message_count` required;
- `skipped`, `collision`, `blocked` excluded;
- external records appended after development/live;
- exact-path winner = existing record unless the later record has corpus `live`.

Current Viewer still declares 439 development + 9 live + 6 registered-external accepted inputs and 453 post-dedup source conversations. The declared external input remains `CONVERSATION_VIEWER/data/discovered_external_conversations.json`, containing 1 manual + 5 discovered records.

Because the diagnostic failed despite the apparent source-code match, the correct next move is to preserve its actual computed report rather than infer which invariant differed.

## Infrastructure repair

Updated `.github/workflows/mercer-cross-source-integrity.yml` so the diagnostic step now:
1. creates `/tmp/mercer-integrity` before executing;
2. pipes the diagnostic JSON through `tee` to `/tmp/mercer-integrity/viewer-input-dedup.json`;
3. retains shell failure propagation, so a real diagnostic mismatch still fails the step;
4. leaves the existing `if: always()` artifact upload able to retain the diagnostic output even when production validation is skipped.

Workflow remains `contents: read`; repository state is not written by the workflow. Commit: `c30dfcd8d52c1189d3b7ea3cddaf0f5051ded975`.

## What this establishes / does not establish

Established:
- Run 41's diagnostic genuinely failed, rather than merely remaining pending;
- regression and frozen competence specimens remained green in that run;
- the failure was not inspectable from retained artifacts because the diagnostic output directory had not yet been created;
- future diagnostic failures now retain the exact computed JSON needed to distinguish accepted-total mismatch, post-dedup mismatch, and duplicate-path provenance.

Not established:
- the exact duplicate path behind 454→453;
- which diagnostic invariant failed in run `34954998631`;
- seventh regression specimen;
- repaired production validator semantics.

## Current frontier / best next operation

Inspect the integrity run triggered by commit `c30dfcd8d52c1189d3b7ea3cddaf0f5051ded975`. Retrieve `viewer-input-dedup.json` from its artifact whether the diagnostic passes or fails. Use the computed duplicate/input/winner data to freeze specimen seven before modifying validator semantics.

## Blockers / dependencies

- Exact diagnostic mismatch awaits the newly retained CI output; transient instrumentation dependency, not a Nathan blocker.
- Historical SAT_CONVOS_15 normalization collision remains unresolved and distinct from this post-acceptance accounting question.
- No workflow/cadence redesign requested from Sable.
