# Mercer Run 34 — 2026-09-14

## Control / boundaries

Read at run start:
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/COMMON/HANDOFFS.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`

Project-wide theory-bearing standdown remains active. This run stayed inside Mercer's read-only metadata/index/source-integrity lane. No raw conversation bodies, tags, curation rules, theory-bearing surfaces, or generated catalogs/manifests were edited.

## Operation

Run 33 left one explicit execution gap: the cross-source integrity validator had passed its synthetic regression fixture but had not been run against a complete current checkout.

The local runtime could not clone GitHub because outbound DNS/network access is unavailable there, so I used the repository's own Actions runtime rather than weakening the check or reconstructing large generated inputs from truncated connector responses.

Created `.github/workflows/mercer-cross-source-integrity.yml` as a read-only CI harness. It checks out the repository, runs the existing regression fixture, runs `WORKSPACES/MERCER/validate_cross_source_integrity.py` against the complete checkout, emits JSON + compact Markdown, and uploads both as an artifact. `permissions: contents: read`.

Initial run `34914794671` failed only because the workflow used guessed CLI aliases (`--root`, `--markdown-out`). The validator's actual CLI is `--repo-root`, `--json-out`, `--md-out`. The regression fixture itself passed. Corrected the workflow immediately; no validator logic changed.

Corrected production run: `34914841055`, commit `a7d9867f5f350207d41319c6274cb02dd3da12ac`.

## Production result

Corrected run completed successfully.

Overall validator classification: **BLOCKED**

- FAIL: 0
- BLOCKED: 1
- WARN: 0
- UNKNOWN: 0
- PASS: 12

The sole BLOCKED result is the already-known development-manifest normalization dependency: 49 blocker records total = 48 `blocked` + 1 `collision`. This is deliberately not classified as data corruption.

All other implemented invariants passed:
- development manifest status arithmetic;
- Viewer ↔ development-manifest generation-marker freshness;
- live manifest status arithmetic;
- no live collision/blocking records;
- Viewer ↔ live-manifest generation-marker freshness;
- Viewer accepted-input arithmetic;
- Viewer catalog/corpus count arithmetic;
- registered-external reconciliation;
- Viewer ↔ manifest record/path/count/timestamp reconciliation;
- Nathan Direct unique+duplicate=input-user arithmetic;
- Nathan Direct yearly-shard arithmetic;
- Nathan Direct → Stage-2 source-record continuity.

Artifact `mercer-cross-source-integrity` (ID `10375348119`) contains `report.json` and `report.md` from the full-checkout run.

## Exact source/state covered

Production checkout commit: `a7d9867f5f350207d41319c6274cb02dd3da12ac`.

Validator consumed its declared metadata surfaces, including:
- `CONVERSATION_VIEWER/data/conversations.json`
- `CONVERSATION_VIEWER/EXTERNAL_CONVERSATIONS.json`
- Viewer-referenced development/live date manifests
- `indexes/nathan-direct/MANIFEST.json`
- `indexes/nathan-direct/stage2/MANIFEST.json`

The pre-run direct checks still showed the established source state: Viewer 374 conversations (365 development / 9 live), development manifest 365 unchanged / 114 skipped / 48 blocked / 1 collision, Nathan Direct 14,306 packaged unique + 7,145 collapsed duplicates = 21,451 user records, Stage-2 source_records 14,306.

## Artifacts changed / created

- CREATED `.github/workflows/mercer-cross-source-integrity.yml`
  - initial commit `bed635528e5124ed8a5e3df46cf07171bf31f7b5`
  - CLI correction commit `a7d9867f5f350207d41319c6274cb02dd3da12ac`
- CREATED `WORKSPACES/MERCER/RUN_034_2026-09-14.md`
- GitHub Actions artifact `mercer-cross-source-integrity`, ID `10375348119`

## Frontier / blockers / discoveries

- The Run-33 full-checkout execution gap is **closed**.
- Current implemented cross-source metadata invariants have zero FAIL/WARN/UNKNOWN on the tested commit.
- The sole operational blocker remains the known SAT_CONVOS_15 collision and its 48 globally blocked rename records; owner disposition remains required before normalization can clear.
- The CI harness now makes future metadata regressions reproducible without requiring Mercer's local runtime to hold a full checkout.
- Because the workflow is path-triggered, changes to the validator, regression fixture, Viewer catalog/registry, date manifests, or Nathan Direct manifests automatically exercise the integrity check.
- The first failed workflow run is retained as an execution-history artifact; it was a harness-argument error, not an integrity failure.

## Best next operation

At next run, reread Control/Common/Handoffs. If canonical navigation maintenance triggered by this run has regenerated manifests/Viewer or the duplicate pair has been dispositioned, verify the changed state first. Otherwise convert the now-production-tested failure specimens into a small archive-worker competence/regression harness, staying metadata/source-integrity only and avoiding Morrow's familiarity-dependent contextual lane.

No Nathan-required decision surfaced in this run.
