# Mercer Run 008 — 2026-09-13

## Control/Common read
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/COMMON/COORDINATION.md`
- `WORKSPACES/COMMON/HANDOFFS.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`

Project-wide theory-bearing standdown remains active. This run stayed in archive/navigation/source-integrity QA.

## Work taken
Followed Morrow's Viewer/catalog handoff rather than repeating scanner QA.

### Root cause found
`indexes/manifests/live-conversation-dates.json` is a dry-run manifest. Eight LIVE records are `status: planned`: `new_path` is only a proposed date-prefixed rename, while the file still exists at `old_path`.

`tools/build_conversation_viewer.py` historically preferred `new_path` unconditionally, causing the Viewer to publish nonexistent repository/raw URLs for those eight records.

### Artifacts changed/created
- Created `tools/build_conversation_viewer_resolved.py` to select a manifest path that actually exists in the checkout, preferring materialized `new_path` and otherwise using existing `old_path`.
- Updated `.github/workflows/build-conversation-viewer.yml` to use the resolver and fail validation if any non-external catalog path does not exist.
- Created `WORKSPACES/MERCER/VIEWER_PATH_QA_2026-09-13.md` with root cause, repair policy, commits, and regression expectations.

### Commits
- `f2f0d8082f55abb3201114d6e6662fdd9e1ac798` — initial resolver wrapper.
- `e70d276ba8d6fd0cf3b52b672eb642daf3b4fd3b` — workflow uses resolver + source-path validation.
- First CI run `34788105816` failed because wrapper fallback called the monkeypatched function recursively.
- `6ef257698796ab34e0442e9e9be6154fe074dc19` — fixed fallback by preserving `_BASE_CANONICAL_PATH` before monkeypatching.
- Successor Viewer run `34788150612` was queued/in progress at run close; CI success is not yet claimed.

## Exact source regions covered
- Morrow `CORE_TEAM_CONVERSATION_CROSSWALK.md` Viewer wayfinding gap.
- `indexes/manifests/live-conversation-dates.json` records, especially all 9 accepted LIVE JSON conversations.
- `CONVERSATION_VIEWER/data/conversations.json` current catalog sample showing prefixed LIVE paths.
- `tools/build_conversation_viewer.py` canonical path selection and build flow.
- `.github/workflows/build-conversation-viewer.yml` generation/validation path.

## Current frontier
Viewer/catalog path drift has a concrete root-cause repair and a durable validation gate. Remaining task is to confirm the successor workflow passes and regenerated Viewer data contains only repository-resolving LIVE paths.

## Blockers/dependencies
- `DEPENDENCY`: successor GitHub Actions run still pending at close; no Nathan decision required.
- Existing `MORROW-SOURCE-001` output-side report provenance remains separate and unresolved.

## Best next operation
At next run, first check Viewer run `34788150612` or successor. If successful, verify all nine LIVE Viewer paths against the tree, update Common/Morrow handoff as resolved, then return to the next eligible Mercer QA branch.
