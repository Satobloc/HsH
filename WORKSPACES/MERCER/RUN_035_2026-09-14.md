# Mercer Run 35 — 2026-09-14

## Control / boundaries

Read at startup: `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`, `BULLETIN_BOARD.md`, `HANDOFFS.md`, and the Mercer checkpoint. The project-wide theory-bearing standdown remains active. Work stayed entirely in metadata/index/source-integrity QA; no raw conversation, tag, theory-bearing surface, generated manifest/catalog, or curation rule was altered.

## Source state checked

The development date manifest remains at generation `2026-09-13T12:30:33.434526+00:00` with 365 unchanged / 114 skipped / 48 blocked / 1 collision. The SAT_CONVOS_15 duplicate-disposition dependency therefore remains open; no new canonical generation was available to verify.

Run 34's production validator result remains the current full-checkout baseline: 12 PASS / 1 BLOCKED / 0 FAIL / 0 WARN / 0 UNKNOWN. The BLOCKED item is the known collision/global-normalization dependency.

## Operation

Converted the known integrity failure classes into a bounded synthetic competence/regression harness: `WORKSPACES/MERCER/test_integrity_failure_modes.py`.

The harness builds metadata-only temporary repository states and checks six specimens:

1. clean baseline -> overall PASS;
2. stale Viewer development-manifest generation marker -> WARN on freshness;
3. Viewer catalog count drift -> FAIL on catalog arithmetic;
4. Nathan Direct package arithmetic drift -> FAIL;
5. Stage-2 source-record continuity drift -> FAIL;
6. declared development collision -> BLOCKED, with no false integrity FAIL.

This directly tests the classification discipline established in Runs 30–34: stale state, arithmetic corruption, and operational blocking are distinct conditions.

Updated `.github/workflows/mercer-cross-source-integrity.yml` so the new harness runs beside the original regression fixture and production validator, and added the harness path to the workflow trigger set.

Commits:
- harness: `7a93fb04f3adc7c943dde80fcfd3510950e31352`
- CI integration: `0723378b7b13cd9fc984c41805fa1e43c5ef69c7`

The push triggered Mercer integrity workflow run `34918942881`; it had entered `in_progress` during this run. Do not treat that transient state as a pass/fail result. Verify its terminal result next run before extending the harness.

## Frontier / blockers

- OWNER ACTION: SAT_CONVOS_15 byte-identical duplicate pair still blocks 48 queued normalization records.
- DEPENDENCY: exact raw IDs for the September 13 Mercer live-source statements remain unavailable.
- DEPENDENCY: machine-readable autotag-side generation/freshness lineage is still unconfirmed.
- AMBIGUOUS_PROVENANCE: glossary / standard-map raw-message ancestry remains unresolved and the current search routes are exhausted pending a stronger anchor.
- No issue from this run requires Nathan attention.

## Best next operation

First verify terminal result/logs for workflow run `34918942881`. If the competence harness passes, freeze these six specimens as the initial archive-worker source-integrity test set and only add new specimens when tied to an observed failure mode. If it fails, repair the harness/validator contract rather than weakening expected classifications. If canonical manifests/Viewer regenerate or the duplicate pair is dispositioned first, verify that changed state before further harness work.
