# Mercer Run 47 — 2026-09-15

## Scope / controls

Read the hard no-conversation-renaming policy first, then worker autonomy, automation control, current coordination/handoffs, Mercer checkpoint, cross-repo execution standard, and shared-state write safety. No quarantine content was accessed. No conversation/thread/chat identity was changed or proposed for change.

## Operation chosen

Continue the confirmed validator-contract repair, but first remove the semantic duplication that caused Runs 41–43 to audit the base-builder rule instead of the production resolved-wrapper rule.

## Work completed

Created `WORKSPACES/MERCER/viewer_input_semantics.py` at commit `e36cea17fd5a22ccc88bcda339a30385d4a18563`.

The module centralizes the already-confirmed production Viewer input contract:

- materialized `new_path` precedes materialized `old_path`;
- supported source suffixes are JSON/TXT;
- `skipped` is Viewer-ineligible, while rename-operation `collision`/`blocked` statuses remain eligible when an existing source resolves;
- Viewer-declared input order is retained;
- the Viewer-declared `registered-external` input is loaded as an input surface rather than assuming the legacy manual registry;
- exact-path dedup keeps the first record unless a later record is `live`;
- output preserves accepted-item, duplicate-path, and winner provenance separately from declared acceptance arithmetic.

This is read-only infrastructure code. It does not parse conversation bodies, alter archive sources, or infer authorship/theory authority.

## Checks / what this establishes

Source-code comparison in Runs 44–46 already established these semantics against `tools/build_conversation_viewer_resolved.py`; Run 46's retained diagnostic computationally confirmed the current 454 accepted -> 453 post-dedup state and exact SAT_CONVOS_15 winner. This run does **not** claim the new shared module is integrated or regression-green yet. Its value is to make the production semantics a single reusable adapter before changing the validator and diagnostic independently again.

## Archive infrastructure change

New reusable QA adapter: `WORKSPACES/MERCER/viewer_input_semantics.py`.

Commit: `e36cea17fd5a22ccc88bcda339a30385d4a18563`.

## Theory / enrichment

No theory work. Capability improvement: the production resolved-input contract is now factored as an explicit reusable abstraction rather than duplicated procedural knowledge, reducing the specific semantic-drift failure mode exposed by Runs 41–44.

## Frontier / blockers / handoffs

Next bounded operation: import this adapter into `validate_cross_source_integrity.py`, replace the stale pre-dedup equality check with separate declared-vs-reconstructed acceptance and reconstructed-post-dedup-vs-Viewer-source cardinality checks, reconcile external records from the Viewer-declared registry, and preserve manifest normalization blockers independently. Then flip specimen seven from expected FAIL to expected PASS and run all seven specimens plus production validation.

The SAT_CONVOS_15 normalization collision remains independently unresolved; Viewer-safe dedup does not disposition it. No Nathan action required.
