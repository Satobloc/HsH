# Mercer Trial Run 5 — 2026-09-13

**Status:** bounded archive/provenance QA under active project-wide theory standdown.

## Startup checks

Read:
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`
- `WORKSPACES/COMMON/scripts/find_superset_conversation_duplicates.py`
- `WORKSPACES/MERCER/SUPERSET_SCANNER_QA_2026-09-13.md`

Standdown remains active. `MORROW-SOURCE-001` remains the highest-priority bounded source-integrity issue explicitly handed to Mercer.

## Work completed

1. Traced the shared superset scanner for a committed invocation or generated report surface.
2. Repository code searches for:
   - `find_superset_conversation_duplicates.py`
   - `SUPERFLUOUS-PREFIX`
   - `Superset Conversation Duplicate Report`
   returned no committed invocation/report surface.
3. Re-read the patched scanner and confirmed the equality gate currently hashes author role/name, the entire `content` object, recipient, channel, and create_time.
4. Independently executed the patched `canonical_message()` logic on two synthetic regression pairs:
   - full `execution_output.text` vs. identical message with an interior `...[truncated]...` marker;
   - otherwise identical message with only `channel` changed.
   Both pairs produced different hashes, so both comparisons correctly fail equality.

## Interpretation / status

This is a successful executable spot-check of the patched equality logic, but not a committed regression suite and not a full archive rerun.

`MORROW-SOURCE-001` remains **partially resolved**:
- scanner equality bug: fixed;
- payload policy: documented;
- Janus regression case: durably documented;
- executable spot-check: passed;
- committed regression fixture: pending;
- stale generated candidate reports: cannot yet be regenerated because no committed invocation/output surface is discoverable.

Any candidate report produced by the old scanner should remain stale. Preserve the earlier Janus export; do not disposition it from pre-patch output.

## Write limitation

An attempt to append this spot-validation result to `SUPERSET_SCANNER_QA_2026-09-13.md` was blocked by the repository write guard. This run note preserves the result without bypassing the guard.

## Current frontier

Under standdown, do not resume ordinary autotag, Viewer, or methodology reconstruction work. If the scanner owner/workflow surface becomes visible, rerun or invalidate affected candidate outputs. Otherwise continue only newly authorized provenance/training/admin work.

## Nathan attention

Not required.
