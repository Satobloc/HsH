# Mercer Run 017 — 2026-09-14

## Scope / controlling state

Read at run start:

- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/COMMON/COORDINATION.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`

Newer explicit Nathan directives remain controlling. Project-wide theory-bearing standdown remains in force. This run stayed within Mercer’s permitted provenance/navigation/source-integrity lane and did not reconcile or promote theory content.

## Navigation-owner check

Performed the one bounded follow-up specified by Run 16 for canonical navigation run `34814002515`.

Result: the run is now **cancelled**, not successful. Therefore it does not establish a fresh canonical manifest regeneration and does not close the stale generated-state dependency. Per the checkpoint rule, Mercer did not spend the run polling/retrying that branch and instead moved to the highest-value safe alternate operation.

Classification remains: owner-execution / generation-lag dependency unless a later successful canonical regeneration shows otherwise. No generated manifest was hand-edited.

## Source-first ancestry/retrieval work

### Repository-native raw-window mechanism identified and inspected

Verified these committed surfaces:

- `.github/workflows/extract-raw-window.yml`
- `WORKSPACES/COMMON/scripts/extract_raw_window.py`
- `WORKSPACES/COMMON/extraction_requests/`
- sample committed request `WORKSPACES/COMMON/extraction_requests/2026-09-13-RAVEL-AFTER-025946.json`

The extractor requires an explicit raw conversation `source_path` plus an `after_create_time`; optional request controls include `user_limit` and `context_each_side`. It parses the raw conversation mapping and emits bounded JSON/Markdown outputs preserving:

- conversation ID/title;
- node ID;
- message ID;
- parent;
- role and author name;
- recipient;
- create time;
- exact extracted text.

The workflow runs all committed request JSONs and commits changed extraction outputs to `WORKSPACES/COMMON/extraction_outputs/`.

### Provenance consequence

This establishes that the repository already has a suitable bounded **raw-message recovery mechanism**. The unresolved glossary/crosswalk ancestry problem is therefore no longer “no raw retrieval tool.” The missing prerequisite is a defensible candidate raw conversation path/time anchor for either:

- `2026/Early SAT/GLOSSARY (LIVE).txt`, or
- `2023-24 FRAMEWORK DEVELOPMENT/SATv  TO STANDARD MAP.txt`.

The extractor is chronological and source-specific, not a corpus-wide phrase-search engine. Creating speculative requests against arbitrary raw files would be low-selectivity and risks duplicating Morrow’s conversation-family lane or Nathan-Words extraction. No speculative extraction request was created.

## Artifacts changed

Updated:

- `WORKSPACES/MERCER/GLOSSARY_CROSSWALK_SOURCE_INVENTORY_2026-09-14.md`
  - added the verified raw-window workflow/script/request/output route;
  - documented exact request prerequisites and output provenance fields;
  - narrowed the ancestry blocker to missing raw source/time anchors;
  - preserved the rule against repeating exhausted GitHub indexed phrase search without changed evidence.

Created:

- `WORKSPACES/MERCER/RUN_017_2026-09-14.md` (this record).

Checkpoint updated separately at run end.

## Exact sources / regions covered

- Common control/coordination surfaces listed above.
- GitHub Actions run `34814002515` status.
- `.github/workflows/extract-raw-window.yml` in full.
- `WORKSPACES/COMMON/scripts/extract_raw_window.py` in full.
- `WORKSPACES/COMMON/extraction_requests/2026-09-13-RAVEL-AFTER-025946.json` in full.
- Existing Mercer glossary/crosswalk inventory and checkpoint.

## Discoveries / questions

### Discovery

Raw-export-aware provenance recovery infrastructure already exists and retains exactly the message-level fields Mercer needs for Nathan Direct attribution once a candidate raw source is identified.

### Remaining question

Which existing raw-export index, title/date correlation, or independently verified candidate conversation path can anchor the two historical definition artifacts? This is a retrieval/provenance question, not a theory question and does not currently require Nathan intervention.

## Blockers / dependencies

- `DEPENDENCY`: canonical navigation run `34814002515` finished cancelled; no successful fresh owner regeneration established this run.
- `DEPENDENCY`: glossary/standard-map raw ancestry still lacks a defensible source-path/time anchor.
- `DEPENDENCY`: historical/manual scanner candidate-report owner/path remains unidentified.
- `DEPENDENCY`: live Nathan UUID/timestamp provenance backfill remains dependent on appropriate raw export surfaces.

## Current frontier

The next useful glossary ancestry step is **anchor discovery**, not another GitHub phrase search and not a broad speculative raw extraction. Prefer an existing raw-export index/catalog/title-date map or a candidate path supplied by independently attributable evidence. Once an anchor is found, use the bounded extractor to recover exact message IDs/timestamps and source text.

For navigation, do not poll run `34814002515` again; it is terminal/cancelled. Revisit the branch only when a newer successful canonical navigation run or landed manifest regeneration appears.

## Best next operation

At the next Mercer run: reread Control/Common; check for new handoffs or a newly successful canonical navigation generation only if there is new evidence. Otherwise inventory the repository’s existing raw-export indexing/catalog machinery for a nonduplicative way to map historical file dates/titles to candidate conversation paths, then test one high-confidence anchor if found. Keep theory-bearing content untouched during standdown.
