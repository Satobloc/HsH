# Mercer Run 27 — 2026-09-14

## Scope

Bounded retrieval/index-integrity QA under the active theory-bearing standdown. No theory synthesis, classification promotion, raw-source editing, or Nathan-words retagging performed.

## Startup / control surfaces read

- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`

Controlling state remains: Mercer may perform bounded archive/navigation/source-integrity/provenance maintenance; theory-bearing work remains frozen.

## Question tested

Commons contains an earlier autotag status snapshot reporting 388 recognized conversation exports / 70,023 message records / 21,499 user messages. Current durable generated outputs report 387 / 69,927 / 21,451. Determine whether the difference is evidence of silent indexing loss or an explainable source-corpus change.

## Sources / regions covered

1. `indexes/autotag/AUTOTAG_SUMMARY.md` current head.
2. Historical `AUTOTAG_SUMMARY.md` at navigation commit `dfba19ae64ccef0ad9a9456499558361279ddd10`.
3. Historical `AUTOTAG_SUMMARY.md` at autotag commits `450dacb0a75ab2a60cf352006b73fc50ff378d71`, `ed04fa2278ab7aac53e7840703f6e025d553b7f0`, and parent `853add4731a3b55d931a360684af993cff12dc00`.
4. Commit-range comparison `dfba19ae64ccef0ad9a9456499558361279ddd10` → `450dacb0a75ab2a60cf352006b73fc50ff378d71`.
5. `indexes/nathan-direct/README.md` and `indexes/nathan-direct/stage2/MANIFEST.json` for downstream count coherence.

## Findings

### 1. The 388 → 387 corpus-count change is source-driven, not unexplained index loss

At `dfba19ae64ccef0ad9a9456499558361279ddd10`, the autotag summary reports:

- recognized conversation exports: **388**
- message records: **70,023**
- user messages: **21,499**
- parse errors: **0**
- structural-index gap candidates: **0**

By `450dacb0a75ab2a60cf352006b73fc50ff378d71`, it reports:

- recognized conversation exports: **387**
- message records: **69,927**
- user messages: **21,451**
- parse errors: **0**
- structural-index gap candidates: **0**

The exact deltas are:

- conversations: **-1**
- all messages: **-96**
- user messages: **-48**

The same commit range contains removal of exactly one raw conversation source:

`DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_6/25.12.29•25.12.30•Court Filing Guidance — raw.json`

The range comparison records that raw source as removed. Its deletion is therefore the concrete source-corpus event explaining the one-conversation reduction; the corresponding autotag output removes 48 user-message records. A placeholder/on-hold markdown file is present in the range, but it is not recognized as a raw conversation export.

This matches the known privacy/legal hold action and should not be treated as a retrieval/index regression.

### 2. Current downstream packaging is count-coherent

Current `indexes/autotag/AUTOTAG_SUMMARY.md` reports **69,927** input message records and **21,451** user messages. Current `indexes/nathan-direct/README.md` reports the same **69,927** input records and **21,451** input user records, then packages **14,306** unique user messages after collapsing **7,145** archive duplicate user records. `14,306 + 7,145 = 21,451` exactly.

Current `indexes/nathan-direct/stage2/MANIFEST.json` reports `source_records: 14306`, matching the packaged unique-user-message count.

No count discontinuity was found across current autotag → Nathan Direct package → Stage-2 queue source count.

### 3. Later apparent scan-count changes are non-conversation inputs / tagger revisions, not corpus-message changes

Historical summaries after the privacy deletion remain at **387 conversations / 69,927 messages / 21,451 user messages** while total JSON scanned rises (414 → 415 → current 416) and non-conversation JSON skipped rises correspondingly (27 → 28 → current 29). This is consistent with additional repository JSON infrastructure being scanned and rejected as non-conversation material.

The latest autotag refresh changes winnow/bucket semantics while retaining the same corpus-message totals. Treat `bulk winnow` and bucket counts as tagger-version outputs, not stable corpus-size invariants.

## QA rule established

When a generated corpus-count snapshot differs from an older Common notice:

1. compare recognized conversation count, all-message count, user-message count, parse errors, and structural-gap count separately;
2. inspect the source-tree change range before labeling the difference as index loss;
3. distinguish raw-source removal/addition from tagger logic changes and added non-conversation JSON inputs;
4. use current generated surfaces as current-state counts; treat Common notices containing counts as dated snapshots unless explicitly declared invariant.

## Artifacts changed / created

- created this run record only;
- updated `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md` separately to carry the result and restart rule.

No generated index, raw conversation, Common historical notice, tagging ledger, or theory-bearing source was edited.

## Current frontier / blockers

- The current autotag → Nathan Direct → Stage-2 count chain is internally coherent for the inspected invariants.
- The historical 388→387 reduction is explained by a known raw-source deletion/hold action, not a demonstrated generator defect.
- Broader selectivity/precision tuning remains outside this run and constrained by the standdown/current active tagging lane.
- Existing Mercer blockers remain unchanged: glossary/crosswalk ancestry needs a stronger source anchor; terminal cancelled navigation run should not be polled; historical scanner report owner/path remains unknown; live-source raw-ID backfill awaits attributable exports.

## Best next operation

On the next run, reread Control/Common. Continue a different bounded implementation/generated-metadata/documentation invariant rather than rechecking these counts. A useful candidate is source-path/count coherence between the current Viewer catalog and its development/live manifests if a fresh owner-generated state has appeared; otherwise choose another nonduplicative owner-facing retrieval invariant. Do not reopen the legal/private source or theory-bearing content.
