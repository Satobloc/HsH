# Mercer Run 54 — Viewer conversation-identity duplicate candidate

**Date:** 2026-09-15  
**Scope:** read-only source/index/Viewer QA; no source disposition, theory work, quarantine access, or conversation-title changes.

## Bounded operation

After the autotag-lineage handoff was routed to Sable/tagging-infrastructure ownership, inspected unresolved archive/source-identity state for a non-owner-blocked QA target. The SAT_CONVOS_15 directory exposed a second duplicate-looking pair not covered by the known byte-identical Cosmological Constant Summary collision:

- `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/4D Topological Model Assessment — raw.json`
- `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/4D Topological Model Assessment — raw (1).json`

## Exact observations

Repository directory metadata on current `main` reports both files at **377? no — 361,104 bytes** but with different blob SHAs:

- plain: `3ae831d52775f6a946a669836ee344b3580eb33d`
- `(1)`: `7bc6ff0c782a797efcf38ff2e3a440db221ca22c`

Both were introduced together by commit `f1bbb754b38fe54c1aed50588177755cfd97e05d` (`Add files via upload`, 2026-09-12T15:34:34Z).

Direct raw-header inspection establishes the same:

- title: `4D Topological Model Assessment`
- `conversation_id`: `69f9ee83-bac4-8330-8460-0ae605478757`
- `create_time`: `1777987215.042467`
- `update_time`: `1789226362.441838`

The visible header difference is ordering of the `safe_urls` array. That observation explains at least one byte-level difference but is **not yet a full semantic-equality result**; no claim is made that array ordering is the only difference.

The current development date manifest independently assigns both records the same start/end timestamps and **43 messages** and plans two different dated destination paths because `(1)` remains part of one basename. Thus filename normalization does not collide or collapse this pair.

The current Conversation Viewer contains **two separate catalog entries** for the pair, with different Viewer IDs (`7ea9d231826c` and `10e3b35b316f`) but the same title, start/end timestamps, and 43-message count. Viewer uniqueness checks are path/Viewer-ID based, so they do not detect duplicate raw `conversation_id` identity.

## What this establishes

This is a **conversation-identity duplicate candidate and a concrete QA blind spot**, stronger than title similarity because the two raw exports declare the same conversation UUID. Current filename/path deduplication is insufficient to detect it, and the Viewer presently exposes both paths as separate conversations.

It does **not** yet establish:

- complete semantic equality of the two JSON exports;
- which file, if either, should survive archive disposition;
- whether one export contains metadata/order differences that should be preserved;
- whether all repeated conversation IDs in the corpus are duplicates rather than legitimate export variants/supersets.

No source should be deleted or normalized away from this result alone.

## Best next operation

Run a full structured comparison of this pair using the Morrow full-content comparator or an equivalent order-aware/order-normalized audit, reporting message IDs/payload changes, graph/branch changes, top-level metadata differences, and set/order-only differences separately. Then scan the development corpus for repeated raw `conversation_id` values and classify exact duplicates, metadata-only variants, prefix/superset exports, and genuinely divergent branches before proposing any Viewer identity policy.

A future Viewer/source-integrity invariant should probably surface repeated raw conversation UUIDs as a typed condition rather than silently treating path uniqueness as conversation uniqueness; policy for variants must be evidence-driven and must preserve provenance.
