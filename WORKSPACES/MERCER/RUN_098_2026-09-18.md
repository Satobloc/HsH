# Mercer Run 98 — sampled extraction branch-locality check

**Date:** 2026-09-18  
**Lane:** Mercer archive/index/retrieval/provenance/documentation QA  
**Operation:** bounded empirical follow-up to Run 97

## Exact sources / coverage

Read/rechecked:
- `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`
- `WORKSPACES/COMMON/WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md` (still through Run 96)
- `WORKSPACES/MERCER/RUN_097_2026-09-17.md`
- `WORKSPACES/COMMON/extraction_outputs/2026-09-14-LAB1-AFTER-130946.json`
- `WORKSPACES/COMMON/extraction_outputs/2026-09-14-LAB1-AFTER-130946.md`

Attempted `AUTOMATION_WORKFLOW_CONTROL.md` at repository root and code search by exact name; no current path was resolved in this run. No quarantine material was accessed. No theory development was performed.

## Result

Run 97 established that `context_each_side` is selected by chronological-list adjacency rather than graph ancestry. This run tested the beginning of one retained extraction that actually used `context_each_side: 1`.

The first visible retained sequence is:

1. assistant `e16c5de4-2537-4f97-bd51-b359a57462db`
2. user target `2356503a-eb62-474e-99da-cd1ff06d80f6`, whose recorded parent is the assistant above
3. assistant `36cdfe32-c841-4a92-8406-e93a224d6065`, whose recorded parent is the user target above
4. next user target `eb4ce2de-8ed7-45c3-af5e-9b54bce91c9a`
5. next assistant `72533254-b0da-4dd2-a7b5-64ecea383b2c`

For the first target window, both chronological neighbors are therefore graph-local by the preserved parent fields: the preceding assistant is the target's parent and the following assistant is the target's child. The rendered Markdown also preserves the same chronological sequence.

## What this establishes / does not establish

**Established:** one exercised `context_each_side: 1` window in the retained LAB1 extraction is branch-local under the output's own preserved graph identity fields. This is a concrete negative sample against the risk identified in Run 97: the implementation can select cross-branch neighbors, but the first sampled window did not.

**Not established:** that the entire LAB1 extraction is branch-local, that all retained extraction outputs are branch-local, or that chronological adjacency is equivalent to graph ancestry. The connector response for this ~45 KB JSON was truncated before all selected rows could be inspected in one bounded read, so I am not promoting a partial sample into a whole-file claim.

## Archive / infrastructure observation

The retained JSON is materially better for this audit than the Markdown rendering because it preserves `parent` per selected row. This supports a cheap future QA method: validate each context row against target ancestry/child relations directly from extraction JSON, without rereading message text. A deterministic validator could classify windows as parent/child-local, same-branch-but-not-immediate, cross-branch, or unresolved.

## Current frontier / best next operations

1. Prefer a small deterministic validator over manual reading if this thread continues; inspect all retained extraction JSONs with positive context and report counts by graph-locality class.
2. Keep Run 97's separate extraction-run provenance issue distinct: retained outputs still warrant an exact checked-out source commit/ref field audit.
3. Do not infer extraction correctness from this one negative sample.

## Continuity note

`TRIAL_CHECKPOINT.md` remains through Run 96. Run 97 is durably recorded separately, and this Run 98 file is the durable continuation. I did not replace the checkpoint wholesale because the available repository write action is whole-file replacement and a safe compact reconciliation should preserve its accumulated state rather than risk destructive continuity loss.
