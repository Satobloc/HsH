# Mercer Run 60 — production v2 identity-signature observation

**Date:** 2026-09-16
**Status:** materially productive archive/provenance QA
**Scope:** read-only observation of the retained production artifact from Actions run `35061454854`; no source, Viewer, filename, or conversation identity disposition.

## Startup / boundaries
Read and followed `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, current Mercer checkpoint, `CROSS_REPO_SCRIPT_EXECUTION_STANDARD.md`, and `SHARED_STATE_WRITE_SAFETY.md`. Conversation renaming remains prohibited. Quarantine was not accessed. No theory work was performed.

## Production observation
Actions run `35061454854` for commit `e83c2c2fae2b5293cece1f4b275b3e363efffd71` completed successfully. Retained artifact: `10432552752`, digest `sha256:c912619b5c8d56f2609e0a354209473ef82c7125395f32845691e598ac814ef3`.

The v2 repeated-conversation diagnostic executed and emitted mapping-internal signatures. This establishes that the new reporting fields execute successfully in production CI; it does not validate source equivalence or authorize deduplication.

## Reclassification of the 12 same-message-ID-set divergent-payload comparisons
All 12 comparisons have:
- identical message-ID sets within each pair;
- zero side-only mapping nodes;
- zero parent/children topology-changed nodes.

So none of these 12 is a graph-branch case under the named topology check. Their divergence is node payload/metadata only.

### Content-bearing signatures
Eleven of the 12 comparisons report at least one `message_content_changed_nodes` node. Ten of those are ordinary content-representation/content payload signatures dominated by `message.content.parts` plus content-type/language/response-format/text fields; one large `AI Enclosure Critique` pair combines 54 changed content nodes with extensive model/tool-response metadata churn.

The content-bearing pairs/families are:
- `SAT Theorizer Emeritus`: 10 changed content nodes in each of two pairwise comparisons; 654 shared mapping nodes; no topology change.
- `AI Enclosure Critique`: 344 changed shared nodes, including 54 changed content nodes; 4,014 shared mapping nodes; no topology change. Additional differences are dominated by `resolved_model_slug` and Spotify/tool-response metadata paths.
- `Population Social Thresholds`: 20 changed content nodes / 472 shared mapping nodes; no topology change.
- `Priority assessment summary`: 26 / 552; no topology change.
- `ChatGPT Voice Glitch`: 1 / 2,413 in each of two comparisons; no topology change.
- `Friday Research Briefs`: 12 content-changed nodes plus one message update-time difference / 355 shared mapping nodes; no topology change.
- `Continue Conversation Here`: 5 / 884; no topology change.
- `H(s)H Archive Audit`: 2 / 901 in each of two comparisons; no topology change.

These signatures establish that raw UUID + identical message-ID set is still insufficient for semantic equivalence. They do **not** establish that every `message.content` difference changes human-visible meaning: some paths explicitly indicate alternate serialization/content representations, and the privacy-preserving diagnostic intentionally retains no differing values.

### Metadata-only signature
`String Theory Particle Zoo` is the clean metadata-only case in this residue: one changed shared node, `message.update_time` only, zero changed message-content nodes, zero topology changes, zero side-only nodes. Its top-level differences are `safe_urls` and `update_time`.

## QA interpretation
The high-risk residue now separates into three distinct problems:
1. **metadata-only export churn** — demonstrated by `String Theory Particle Zoo`;
2. **same graph / same IDs / content-bearing node differences** — the remaining 11 comparisons, requiring bounded value-level ancestry inspection before any equivalence claim;
3. **graph-cardinality/overlap divergence** — the four Run-58 divergent cases, still branch/snapshot ancestry candidates and outside the same-ID-set residue.

A safe identity policy therefore cannot deduplicate on raw `conversation_id`, message-ID set, or graph topology alone. Viewer suppression/disposition remains out of scope.

## Enrichment / capability change
The v2 diagnostic now demonstrates a useful layered equivalence test: artifact bytes -> canonical JSON -> raw conversation UUID -> message-ID set -> graph topology -> content payload -> metadata. This sharply reduces the amount of value-level source comparison needed while preserving privacy in retained CI artifacts.

## Current frontier / best next operations
1. Perform bounded value-level comparisons only for the 11 content-bearing comparisons, preferably hashing/categorizing normalized human-visible content separately from representation metadata rather than retaining bodies.
2. Determine whether the recurring `content_type/language/parts/response_format_name/text` pattern is a systematic serialization migration rather than human-visible content divergence.
3. Treat `AI Enclosure Critique` separately because tool-response/model metadata churn is unusually large.
4. Keep the four graph-divergent cases on the ancestry/branch track.
5. Do not add suppression semantics locally; consult Sable if identity policy/workflow ownership changes are proposed.

## Blockers / handoffs
No Nathan action required. No new blocker. No workflow redesign requested from Sable yet; evidence gathering remains high-yield inside Mercer QA.
