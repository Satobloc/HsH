# Mercer Run 41 — 2026-09-15

## Scope / controlling surfaces

Read first and obeyed:
- `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`
- `WORKSPACES/COMMON/WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- current `COORDINATION.md` and `HANDOFFS.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`

Before touching workflow/script state also read:
- `WORKSPACES/COMMON/CROSS_REPO_SCRIPT_EXECUTION_STANDARD.md`
- `WORKSPACES/COMMON/SHARED_STATE_WRITE_SAFETY.md`

No conversation/thread/chat title was changed or proposed for change. No quarantine material was accessed.

## Bounded operation

Continued Runs 39–40's Viewer/validator contract audit. The current Viewer declares 439 accepted development + 9 accepted live + 6 accepted registered-external inputs, but 453 source conversations after exact-path deduplication. The external source is the six-record generated discovery registry, not the one-record manual registry.

Direct connector inspection could establish the accounting contract but not safely enumerate all 560 development-manifest canonical paths in one local computation. Rather than guess the duplicate path, added a narrow repo-native read-only diagnostic that runs against a full checkout.

## Artifact added

`WORKSPACES/MERCER/diagnose_viewer_input_dedup.py`

Properties:
- reads only `CONVERSATION_VIEWER/data/conversations.json` and the metadata input paths declared by its `inputs[]` array;
- does not open raw conversation bodies or descend repository trees;
- reconstructs builder acceptance rules for date-manifest records;
- loads the Viewer-declared registered-external registry rather than a hardcoded registry path;
- replays builder exact-path precedence (`existing wins unless later corpus == live`);
- prints every duplicate path with all contributing input records and the selected winner;
- exits nonzero if reconstructed accepted total or post-dedup total disagrees with Viewer declarations.

Commit: `b179ba37603e3322631182865c752ca79f7b2c4e`.

## CI integration

Updated `.github/workflows/mercer-cross-source-integrity.yml` to:
- trigger on changes to the new diagnostic and the generated discovered-external registry;
- run the dedup diagnostic after the frozen regression/competence fixtures and before production validation.

Workflow remains `contents: read` and the diagnostic itself is read-only. Commit: `d98722ccf242e6dfa1b8c2a79521466ef160ae1f`.

Triggered integrity run: `34954998631`. At end of this Mercer run it was still in progress during checkout; no diagnostic result or green status is claimed yet.

## Additional source check

The known SAT_CONVOS_15 `Cosmological Constant Summary` normalization collision remains explicit in the current development manifest and is excluded by builder acceptance because its status is `collision`. Therefore it must not be casually identified as the separate 454→453 Viewer dedup event without the full-input computation.

## What this establishes / does not establish

Established:
- there is now a deterministic full-checkout route to identify the exact current Viewer duplicate without guessing;
- the route follows the Viewer-declared external lineage and builder precedence semantics;
- CI now treats changes to the generated discovered-external registry as relevant to Mercer integrity QA.

Not yet established:
- exact duplicate path behind the current 454→453 reduction;
- repaired validator semantics;
- seventh regression specimen.

## Archive infrastructure / capability change

Mercer's source-integrity tooling can now inspect post-acceptance path collisions independently of the stale validator. This is useful beyond the immediate defect because future external-discovery expansion can be audited without conflating accepted-input counts with post-dedup catalog counts.

## Current frontier / best next operation

When Actions run `34954998631` reaches the diagnostic step, capture the exact duplicate path/input provenance. Freeze that observed semantics as a seventh regression specimen. Then repair `validate_cross_source_integrity.py` coherently: resolve the external source from Viewer `inputs[]`, reconstruct accepted records, replay exact-path precedence, and compare accepted totals, post-dedup totals, winner provenance, and metadata separately. Keep the original six specimens unchanged.

## Blockers / dependencies

- Diagnostic workflow result pending; transient, not a Nathan blocker.
- Historical SAT_CONVOS_15 normalization collision remains unresolved; no Nathan decision currently required.
- No workflow/cadence redesign requested from Sable.
