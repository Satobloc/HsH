# Mercer Run 055 — 2026-09-15

## Scope
Follow-up to Run 54's repeated-`conversation_id` candidate in `SAT_CONVOS_15`: the two `4D Topological Model Assessment` raw exports associated with conversation UUID `69f9ee83-bac4-8330-8460-0ae605478757` and blob SHAs `3ae831d52775f6a946a669836ee344b3580eb33d` / `7bc6ff0c782a797efcf38ff2e3a440db221ca22c`.

## Control / policy reads
Read `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`, `WORKSPACES/COMMON/WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`, and `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md` before operating. No conversation/thread/chat identity was renamed, retitled, or otherwise altered.

## Work completed
Fetched both raw blobs directly by SHA from `Satobloc/HsH` rather than inferring from Viewer/catalog metadata. The second blob (`7bc6ff0...`) was inspectable in full and confirms the root identity metadata recorded in Run 54: title `4D Topological Model Assessment`, conversation UUID `69f9ee83-bac4-8330-8460-0ae605478757`, create time `1777987215.042467`, update time `1789226362.441838`, and a single conversation mapping extending through the later survey/check-in exchange.

The two Git blob SHAs remain different. Run 54 had already established equal byte size, equal 43-message count, equal manifest start/end timestamps, and visible `safe_urls` ordering differences. This run therefore strengthens the interpretation that the pair are two serializations/exports of one underlying ChatGPT conversation identity rather than two independent conversations. It does **not** yet establish byte-normalized or semantic identity, because a field-by-field machine diff of the two complete JSON objects has not been executed.

A repository code-search query for the raw conversation UUID returned no indexed hit. That is an indexability limitation, not evidence that the files are absent; direct blob fetch succeeds and remains the authoritative source read for this bounded audit.

## Checks and what they establish
- Direct SHA fetch of both blobs: both source objects exist and are readable from the repository.
- Full inspection of `7bc6ff0...`: confirms the raw ChatGPT `conversation_id` and coherent mapping identity, including the late survey/check-in continuation.
- Cross-check against Run 54: same UUID + same title/timestamps/message cardinality + same manifest temporal bounds + equal byte size, despite different blob SHA.
- **Established:** source-identity duplication candidate is strong and Viewer-ID uniqueness is insufficient as a conversation-identity invariant.
- **Not established:** exact semantic equality; prefix/superset relation; which export, if either, should be canonical; whether metadata-only differences are limited to `safe_urls` ordering.

## Archive / Viewer implication
Do not disposition or suppress either source yet. The correct next test is a deterministic structured JSON comparison that separates:
1. message graph identity/content,
2. root conversation metadata,
3. message metadata/tool traces,
4. order-insensitive URL/list metadata,
5. attachments/file references,
6. any true divergent branch or appended content.

If the message graph and substantive metadata are identical after explicitly documented normalization, this pair should be classified as a metadata/serialization duplicate candidate while preserving both raw artifacts and ancestry. If one graph is a strict prefix/superset or branch variant, Viewer/catalog policy should preserve that relationship rather than collapse solely on UUID.

## Infrastructure observation
The current Viewer can expose two records with different generated Viewer IDs even when their raw source objects share the same underlying ChatGPT `conversation_id`. Identity-level QA therefore needs a corpus-wide repeated-UUID diagnostic before any enforcement policy is considered. The diagnostic should report, not mutate.

## Enrichment / capability growth
Refined the identity model into three distinct layers that should not be conflated: raw export artifact identity (blob/path), ChatGPT conversation identity (`conversation_id`), and Viewer presentation identity (generated Viewer ID). A collision at one layer does not by itself determine disposition at another.

## Frontier / next operations
1. Execute deterministic structured diff of the two `4D Topological Model Assessment` blobs, with normalization rules explicitly reported rather than silently applied.
2. Build/read-only corpus-wide repeated-`conversation_id` inventory and classify groups as exact, metadata-only, prefix/superset, branch-divergent, or unresolved.
3. Only after classification, propose Viewer/catalog identity QA that reports source ancestry without deleting or hiding preserved raw exports.

## Blockers / handoffs
No Nathan action required. No theory work performed. No quarantine material accessed. Any eventual Viewer identity-policy change should be coordinated through the appropriate workflow owner/Sable; Mercer can supply the diagnostic and audit contract.