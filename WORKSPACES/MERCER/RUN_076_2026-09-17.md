# Mercer Run 76 — 2026-09-17

## Scope

Bounded archive/index normalization QA. Rechecked the long-open SAT_CONVOS_15 normalization collision because the generated development manifest has materially changed since Run 29. No source disposition, rename, deletion, Viewer change, theory work, or quarantine access was performed.

## Startup/control surfaces read

- `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`
- `WORKSPACES/COMMON/WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/COORDINATION.md`
- `WORKSPACES/COMMON/HANDOFFS.md`
- `WORKSPACES/SABLE/INBOX.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`
- before repository writes: `WORKSPACES/COMMON/CROSS_REPO_SCRIPT_EXECUTION_STANDARD.md` and `WORKSPACES/COMMON/SHARED_STATE_WRITE_SAFETY.md`

The Run-71 Viewer relation-sidecar request remains OPEN in Sable's inbox; no approval or implementation was inferred.

## Exact sources / coverage

1. Current `indexes/manifests/development-conversation-dates.json`, blob `76456ceda8dbdeff4590fba0e9520ba850559712`, generated `2026-09-17T01:30:01.653259+00:00`, mode `dry-run`.
2. Current `tools/date_conversation_exports.py`, blob `aed82bb3b6079952e5aee4a9ffe349d124bd3398`, especially `build_records()` and `main()` collision/apply semantics.
3. Both SAT_CONVOS_15 `Cosmological Constant Summary` raw paths from Run 29:
   - `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/Cosmological Constant Summary — raw.json`
   - `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/26.06.22•26.09.12•Cosmological Constant Summary — raw.json`
   Both still resolve to blob `40dd17e8d6114d65f54e5712ff8ee4e5fc3812a0`.
4. Run 29 durable diagnosis for historical comparison.

## Finding

The duplicate collision itself remains unresolved: both byte-identical SAT_CONVOS_15 paths still exist, and the current manifest still reports exactly one collision.

However, the current manifest summary is now:

- 364 unchanged
- 121 skipped
- 74 planned
- 1 collision

This must **not** be read as evidence that the former global apply blocker was removed. The current manifest is explicitly a **dry run**. The current script still implements the all-or-nothing apply guard: if `--apply` is used and any collision exists, every `planned` record is converted to `blocked` with `No files renamed because at least one collision exists`; the process returns nonzero when blockers exist.

Therefore the correct current state is:

> **one unresolved exact-byte collision + 74 currently planned dry-run renames that would all become blocked under apply while that collision remains.**

This supersedes the stale quantitative wording in the old handoff/checkpoint that said the collision "leaves 48 other rename records blocked." The underlying dependency is unchanged, but the affected candidate set has grown from 48 to 74 and current generated output is dry-run rather than apply-mode.

## What the check establishes / does not establish

Establishes:
- the exact duplicate pair still exists byte-identically;
- the current generated manifest still detects one collision;
- 74 current rename candidates are planned in dry-run state;
- current script semantics would block those candidates in apply mode while the collision exists.

Does not establish:
- which duplicate path should be retained or removed;
- authority/currentness of either source;
- that any rename should be executed now;
- that Viewer presentation is incorrect;
- theory correctness or source-content equivalence beyond exact byte identity of this pair.

## Archive-infrastructure change

No production infrastructure changed. This run repairs the *state description* of the normalization dependency so future workers do not confuse dry-run `planned` with apply-mode readiness.

## Current frontier / blocker

`OWNER ACTION / RECHECK` remains: provenance-preserving disposition of the exact duplicate pair by the owning archive/index-maintenance lane, followed by canonical regeneration. The measurable impact is now 74 current rename candidates, not the historical 48.

## Workflow observation for Sable

Generated-manifest mode is semantically important. Status counts cannot be compared across runs without carrying `mode`; `planned` in dry-run and `blocked` in apply can describe the same unresolved collision gate. Any Dashboard/rollup of normalization health should preserve mode alongside counts.

## Handoffs/questions

No new handoff required; the existing archive/index-maintenance handoff remains valid after this quantitative correction. No Nathan action required.

## Best next operation

Do not recheck this collision again until the duplicate paths, dating script, or generated manifest materially changes. On the next run, inspect Sable's Viewer-sidecar response state; if still open, choose a different bounded archive/retrieval/provenance invariant.
