# Mercer Run 46 — 2026-09-15

## Scope
Read current hard no-conversation-renaming policy, worker autonomy/control, Common coordination/handoffs/check-ins, Mercer checkpoint, and write-safety surfaces. No quarantine access. No conversation/thread/chat identity was changed or proposed for change.

## Operation
Retrieved the retained artifact from Mercer cross-source integrity run `34977962790`, triggered by diagnostic repair commit `4ec472ff2e78724a0cf8e9981af7e9c35863666a`. Workflow check `validate` completed successfully. Artifact `10399888652`, digest `sha256:4f78f568ff2ed9cee9ab26a90810ce0af2d4b84fa9612923eeff2116f2df5c40`, contains `viewer-input-dedup.json`, `report.json`, and `report.md`.

## Exact diagnostic result
The repaired production-resolved diagnostic reconstructs:
- accepted inputs: **454**;
- post-dedup records: **453**;
- exactly **one duplicate path**;
- semantics: `production-resolved-existing-source-v1`.

Duplicate path:
`DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/26.06.22•26.09.12•Cosmological Constant Summary — raw.json`

Two manifest inputs resolve to that same path:
1. source index **253**, status `unchanged`, old/new/path all the dated path, resolved from `new_path`, 55 messages;
2. source index **257**, status `collision`, old path `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/Cosmological Constant Summary — raw.json`, new path the dated path, resolved from existing `new_path`, 55 messages.

Winner under production exact-path precedence is source index **253**, the earlier `unchanged` manifest record. The later collision record does not replace it because it is not `live`.

This confirms the corrected Run-44/45 model and closes the diagnostic-execution dependency. The Viewer metadata arithmetic is coherent under production resolved semantics. Viewer-safe deduplication still does **not** disposition the underlying byte-identical archive duplicate or clear the manifest normalization blocker.

## Validator state
The same artifact shows the production validator still reports overall `FAIL` solely because `viewer.accepted_input_arithmetic` compares the pre-dedup accepted total 454 directly with post-dedup `source_conversations_before_curation` 453. The separate manifest collision remains correctly `BLOCKED`; all other reported checks pass. This is now a reproducible validator-contract defect, not a Viewer/catalog defect.

The validator repair remains bounded: reconstruct production-resolved accepted records, replay exact-path dedup/winner provenance, follow the Viewer-declared external input, compare pre-dedup and post-dedup cardinalities separately, and retain normalization collision status independently.

## Concurrent navigation observation
The navigation run spawned from the later Run-45 checkpoint commit was cancelled during the long autotag step after a newer repository push; it produced no artifact. A newer navigation run was already in progress and had successfully rebuilt Viewer/chronology before entering autotag. This does not affect the retained integrity diagnostic, which ran successfully against commit `4ec472f...` and preserved its exact input state.

## Checks establish / do not establish
Established: exact current duplicate path, both manifest antecedents, resolution route, winner provenance, accepted/post-dedup cardinality, and the validator's remaining arithmetic mismatch.

Not established: provenance-preserving disposition of the two archive files; theory correctness/currentness; authorship beyond existing metadata; any quarantine content.

## Next operations
1. Repair `validate_cross_source_integrity.py` against the now-confirmed production-resolved contract.
2. Flip specimen seven to expected PASS only after that repair; rerun all seven specimens plus production validation.
3. Keep the SAT_CONVOS_15 normalization collision separately `BLOCKED` pending explicit provenance-preserving duplicate disposition.
