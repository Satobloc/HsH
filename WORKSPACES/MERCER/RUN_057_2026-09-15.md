# Mercer Run 57 — repeated conversation-identity diagnostic observation

**Date:** 2026-09-15  
**Role:** archive/index/retrieval/provenance/documentation QA  
**Status:** materially productive; reporting-only identity QA  

## Startup / boundaries
Read `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `AUTOMATION_WORKFLOW_CONTROL.md`, `COORDINATION.md`, `CROSS_REPO_SCRIPT_EXECUTION_STANDARD.md`, `SHARED_STATE_WRITE_SAFETY.md`, and `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`. No conversation/thread/chat title was changed or proposed for change. Quarantine was not accessed. No theory-bearing work was performed.

## Exact source / execution state
Observed GitHub Actions run `35049604577` (`Mercer cross-source integrity`) at head SHA `309b42f72ce4ddb94ef2ac0e5b1494fe7c6a583c`. Run status: completed / success. Retained artifact: `mercer-cross-source-integrity`, artifact id `10427898109`, digest `sha256:a48f7db9e4302af7483acff7b5168486679dc72e707051ff1bae65123d0013c8`.

Inspected retained `conversation-identity-duplicates.json`, generated by `WORKSPACES/MERCER/diagnose_conversation_identity_duplicates.py` over Viewer-resolved accepted JSON inputs.

## Corpus result
The diagnostic scanned **412 JSON conversations carrying a raw `conversation_id`** and found **74 repeated-conversation-ID families**. No input was unreadable.

This is substantially broader than the Run-54 specimen and establishes that raw conversation identity repetition is a corpus-level archival condition, not an isolated SAT_CONVOS_15 anomaly.

Pairwise classifications across the 74 families include:
- 56 `exact-bytes` pairs;
- 18 `top-level-order-only` pairs;
- 17 `same-message-graph-top-level-metadata-diff` pairs;
- 66 `message-id-prefix-or-subset-candidate` pairs;
- 49 `message-id-superset-candidate` pairs;
- 12 `same-message-id-set-divergent-payload` pairs;
- 4 `divergent-branch-or-unclassified` pairs.

These are pair counts, not family counts; multi-artifact families contribute multiple pairwise comparisons and may contain more than one relation class. At family level, 23 families are exact-bytes-only; six are top-level-order-only-only; five are same-message-ID-set-divergent-payload-only; two are divergent/unclassified-only. Other families contain prefix/superset or mixed relations.

## Run-54 target resolved
Raw conversation UUID `69f9ee83-bac4-8330-8460-0ae605478757`, title `4D Topological Model Assessment`, occurs in:
- `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/4D Topological Model Assessment — raw (1).json`
- `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_15/4D Topological Model Assessment — raw.json`

The retained diagnostic classifies this pair **`top-level-order-only`**. Both have 53 message nodes, all 53 message IDs are shared, and their mapping hashes are identical (`fed69d95b72bdad274516169d68e649597dbf326c9c76311399227f4c8a19fb3`). The only differing top-level key is `safe_urls`; byte and canonical-JSON hashes differ because list ordering differs.

This closes the bounded semantic-classification question from Runs 54–56: the pair is two serializations/exports of the same raw conversation graph with order-only top-level metadata variation. It does **not** by itself authorize deletion, archive disposition, or Viewer suppression.

## Additional high-value signal
The corpus report also confirms several materially different repeated-ID modes that make a blanket `conversation_id` dedup rule unsafe. Examples include exact duplicate exports across SAT_CONVOS directories, later exports that are strict message-ID supersets of earlier exports, same-ID-set divergent payloads, and a small divergent/unclassified residue. Therefore raw UUID equality is a strong identity/provenance key but not a sufficient content-equivalence predicate.

## What the checks establish / do not establish
Established:
- the Run-56 CI integration executed successfully;
- repeated raw UUIDs are common enough to require explicit archival semantics;
- the 4D Topological Model Assessment pair is order-only metadata variation over an identical message graph;
- a reporting-only identity invariant is justified and should preserve relation class rather than flatten all repeated UUIDs into duplicates.

Not established:
- which archive artifact should be retained/dispositioned in any family;
- that prefix/superset candidates are safe to collapse without content-level comparison and provenance policy;
- that same-message-ID-set divergent payloads are benign;
- that Viewer should suppress repeated UUIDs automatically;
- any theory correctness/currentness/maturity claim.

## Archive-infrastructure / capability change
Mercer now has a retained corpus-wide taxonomy surface separating artifact identity, raw conversation identity, message-graph/content relation, and Viewer presentation identity. This materially improves duplicate/prefix/superset QA and gives future Viewer/catalog policy a reproducible evidence base.

## Frontier / handoff
Best next operation: convert the observed corpus taxonomy into a **reporting-only identity QA invariant/summary**, not suppression behavior. Priority should be the 12 same-message-ID-set divergent-payload pairs and four divergent/unclassified pairs, because they carry the highest risk of silently treating non-equivalent exports as duplicates. Any cross-lane Viewer behavior/policy change should be proposed to Sable rather than silently implemented.

The existing SAT_CONVOS_15 Cosmological Constant Summary normalization collision remains a separate source-disposition issue.

## Nathan attention
None required.
