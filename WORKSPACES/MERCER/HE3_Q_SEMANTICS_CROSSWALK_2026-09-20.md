# He-3 Q Semantics Crosswalk — source-first

**Date:** 2026-09-20  
**Worker:** Mercer  
**Status:** provenance/reconstruction aid; not theory authority, mathematical validation, or physical validation  
**Primary source inspected:** `Satobloc/SAT_THEORY_ARCHIVE_2023-25/2026/SAT CORE — HELIUM STANDARD ATOM 2.txt`

## Purpose

Keep distinct the incompatible-looking uses of `Q` inside the same He-3 source instead of normalizing them into one quantity. This crosswalk records what the source says and the level/object each occurrence appears to count. It does not decide whether the later use is an intentional coarse-graining, a changed convention, or an internal inconsistency.

| Record | Q definition / level | Object counted | Source wording / context | Mass stage / formula | Angular quantity nearby | Reconstruction status |
|---|---|---|---|---|---|---|
| A | `Q=3` subunit / local triplet | one nucleon modeled as a stable Borromean triplet; source also describes three adjacent `Q=3` vertices | Opening topology block: three nucleons, each a stable Borromean triplet (`Q=3`); UV lock caps stable ground-state intersections at `n≤3`. Next section says He-3 has exactly three Borromean triplets (`Q=3`). | Opening block describes effective mass as topological + induced inertial mass and later audits proportional `m ∝ Q`; no new derivation performed here. | `B=3/(4π)≈0.2387 rad`; `θ4` appears as misalignment angle. | Explicit source usage. Mathematical claims remain historical/source claims; no new validation status assigned. |
| B | `Q_total=9` nuclear total | all nine primary filaments of He-3 | Opening topology block explicitly gives total topological charge `Q=9`; three `Q=3` nucleon triplets. Next section explicitly says the three triplets weave a total of nine primary filaments. | Same early proportional-mass context; source later flags a `Q=9` scaling bottleneck / complexity-inversion risk. | Same early `B≈0.2387 rad` / `θ4` vocabulary. | Explicit source mapping from three local/subunit Q=3 triplets to total Q=9. |
| C | later whole-anchor `Q=3`; `Q` redefined as integrated topological charge / effective intersection density | whole He-3 holotype / isotope-scale ensemble entry | Later “He-3 Holotype: Geometric Anchor” calls He-3 a fermionic `Q=3` bundle. Later mass-scaling section defines Q as integrated topological charge/effective intersection density rather than simple nucleon count and tabulates He-3 anchor `Q=3.0`. | Later block states `m_eff ≈ m0/Q` and tabulates isotope values. This conflicts in direction/convention with the earlier proportional-Q language unless an unstated stage/redefinition relates them. | Later block uses `θ_F≈0.239 rad` as projection/filament-angle constant and separately `θ4` as misalignment. | Explicit later source usage, but its relation to A/B is unresolved. Do not silently identify it with nucleon-triplet Q=3 or total Q=9. Formula is not newly validated/disclaimed. |

## What is established by the source

The source itself explicitly supports a two-level count in its opening material: three `Q=3` Borromean nucleon/triplet units make a He-3 total of nine primary filaments and `Q_total=9`. That distinction is therefore not merely an assistant inference.

The later isotope-scaling block introduces a third usage: the *whole* He-3 anchor is labeled `Q=3`, while Q is described as integrated topological charge/effective intersection density and mass is written inversely in Q. The inspected source does not, in the passages recovered so far, explicitly map that later quantity onto the earlier local-triplet and total-filament quantities.

## Do not infer

Do not infer that later whole-anchor `Q=3` is automatically the same variable as local/nucleon `Q=3`. Do not replace `Q_total=9` with `Q=3`, or vice versa. Do not infer that `m∝Q` and `m_eff≈m0/Q` refer to the same mass stage or are mutually repaired. Do not treat the source's polished language as mathematical or physical validation.

## Next adjudication target

Highest-value source remains `2026/SAT AUDIT — Q COUNTING CONCERNS.pdf` (known binary PDF; previously not readable through the GitHub UTF-8 route). Prefer an existing text/extraction counterpart or another reliable non-OCR route. The adjudication question is narrow: does any source explicitly define the transformation or coarse-graining, if any, between local triplet/intersection Q, total nuclear-filament Q, and later integrated/effective Q?
