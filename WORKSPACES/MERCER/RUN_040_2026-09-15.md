# Mercer Run 40 — 2026-09-15

## Scope / controlling surfaces

Read first and obeyed:
- `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`
- `WORKSPACES/COMMON/WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- current `COORDINATION.md` and `HANDOFFS.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`

No conversation/thread/chat title was changed or proposed for change. No quarantine material was accessed.

## Bounded operation

Continued Run 39's Viewer/validator contract audit source-first. Read:
- `tools/build_conversation_viewer.py`, especially `build_manifest()` input ordering and exact-path deduplication;
- current `CONVERSATION_VIEWER/data/conversations.json` input metadata;
- current `CONVERSATION_VIEWER/data/discovered_external_conversations.json`;
- manual `CONVERSATION_VIEWER/EXTERNAL_CONVERSATIONS.json`;
- `WORKSPACES/MERCER/validate_cross_source_integrity.py`;
- current development/live date-manifest metadata.

## Material finding

Run 39 correctly established that accepted inputs are deduplicated by exact `path` after development, live, and external records are appended, with a later record replacing an existing same-path record only when the later record's corpus is `live`.

Run 40 found an additional validator-lineage defect that must be repaired before the arithmetic fix can be trusted:

1. The current Viewer declares its registered-external input as `CONVERSATION_VIEWER/data/discovered_external_conversations.json`, with 6 accepted records.
2. That generated discovery surface contains 1 manual registration plus 5 structurally discovered public cross-repo records.
3. Mercer's validator does **not** follow the Viewer-declared external input path. It hardcodes `CONVERSATION_VIEWER/EXTERNAL_CONVERSATIONS.json`, which currently contains only the single manual Srena record.
4. Therefore the validator's external path set and `viewer.external_registry_reconciliation` are auditing a different external source surface from the one the Viewer actually consumed.

This explains why simply repairing accepted-count arithmetic would be insufficient: the validator must first derive the external registry from the Viewer's own `inputs[]` metadata, then reproduce builder precedence over the exact input records.

## What this establishes / does not establish

Established from current source and generated metadata:
- Viewer builder input order and exact-path precedence semantics.
- Current Viewer external lineage points to the six-record discovered registry.
- Validator external lineage is stale/hardcoded to the one-record manual registry.

Not established this run:
- the exact path responsible for 454 accepted inputs collapsing to 453 post-dedup records;
- whether that collision is development↔development, development↔live, manifest↔external, or another exact-path duplicate;
- any corruption or theory/content conclusion.

The 454→453 reduction remains consistent with builder semantics, but exact collision identity should be computed from the same normalized input surfaces rather than guessed from titles or corpus labels.

## Artifacts changed

Created this run record only. No raw conversation, generated Viewer/catalog, manifest, tag, curation rule, or theory-bearing source was changed.

## Current frontier / best next operation

Repair design should be one coherent change, not two ad hoc exceptions:

1. resolve the external input path from Viewer `inputs[]` where `corpus == "registered-external"`;
2. normalize accepted development/live manifest records with the same canonical-path/status rules as the Viewer builder;
3. append external records from the Viewer-declared external source;
4. replay the builder's exact-path precedence to obtain expected surviving records and provenance-of-winner;
5. compare accepted-total, deduplicated-total, surviving source provenance, and metadata separately;
6. add one observed-contract regression specimen containing manifest records plus a same-path collision and multiple external records;
7. keep the original six competence specimens frozen and green.

If practical before patching, use a local/full-checkout computation to print the exact duplicate path(s) from the current inputs and retain that result in the regression specimen.

## Blockers / dependencies

- Historical SAT_CONVOS_15 manifest collision remains unresolved; no Nathan decision currently required.
- Exact current Viewer dedup collision path remains to be computed.
- No workflow/cadence redesign requested from Sable.
