# Mercer Run 104 — 2026-09-18

## Scope
One-object read-only extraction-context QA on `WORKSPACES/COMMON/extraction_outputs/2026-09-13-RAVEL-AFTER-025946.json` (blob `1f85fb29d7fda26bcaddb40c363b0c883fdd2a9e`). No source/output/theory/quarantine mutation.

## Exact source / request
The retained extraction identifies source `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_13/26.09.06•26.09.10•🎼 Ravel [H(s)H] Syncmathcek — raw.json`, conversation id `6a9d1a23-4fac-83ea-b76f-6805ece3ee9f`, `context_each_side: 1`, user limit 24.

## Check performed
Inspected the first retained timestamp-ordered segment and compared each visible row's `node_id` against the following row's recorded `parent` rather than assuming chronological adjacency is graph ancestry.

Visible sequence begins:
- assistant `10abe1d8-...`, parent `0d33cca0-...`;
- user `1896b541-...`, parent `10abe1d8-...` — graph-local edge;
- assistant `5daa8e99-...`, parent `47d433c5-...` — **not** child of preceding user `1896b541-...`;
- user `a7a7f141-...`, parent `5daa8e99-...` — graph-local edge;
- tool `b61a170e-...`, parent `25c75da6-...` — another visible splice relative to preceding user.

## Result
Positive reproduction outside LAB1 and RUSSIA. The RAVEL retained output contains timestamp-adjacent rows that do not form one continuous parent/child path. This independently confirms that the extraction-context issue is not confined to the previously sampled conversation families. It also reinforces the Run-100/101 criterion: target-local predecessor/parent correctness is insufficient; whole retained context-segment continuity must be checked.

This establishes extraction-context provenance/semantics divergence in this bounded visible segment only. It does **not** establish file-wide or corpus-wide prevalence, downstream interpretive impact, source corruption, or theory correctness.

## Archive/infrastructure consequence
A deterministic classifier remains the right next infrastructure operation. Categories should include immediate graph-local, same-branch/non-immediate, cross-branch/spliced, timestamp-order inversion, and unresolved. Preserve historical extraction outputs unchanged; classification should be additive/read-only.

## Enrichment / capability
Cross-family sampling now covers LAB1, RUSSIA, and RAVEL and demonstrates why graph-continuity QA must operate on complete retained segments rather than only target-neighbor pairs.

## Handoff / workflow observation
Existing `EXTRACTION CONTEXT / OWNER-SABLE` routing remains sufficient. No Nathan decision is required. Do not redesign the extraction lane locally.

## Next cursor
Move from example accumulation to deterministic prevalence measurement across retained positive-context JSON outputs, ideally via a read-only classifier that reports segment-level graph continuity without rewriting historical outputs.