# Mercer Run 65 — 2026-09-16

## Scope / authority read
Read `WORKSPACES/COMMON/NO_CONVERSATION_RENAMING_POLICY.md`, `WORKER_AUTONOMY_HANDOFF_PROTOCOL.md`, `CROSS_REPO_SCRIPT_EXECUTION_STANDARD.md`, `SHARED_STATE_WRITE_SAFETY.md`, and `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`. Attempted root `AUTOMATION_WORKFLOW_CONTROL.md`; it is not present at that path and repository code search returned no match. No quarantine access. No theory work.

## Operation
Implemented `WORKSPACES/MERCER/diagnose_conversation_identity_duplicates_v4.py` as a read-only extension of the production-green v3 diagnostic.

V4 repairs the Run-64 denominator defect by counting text comparability only among nodes whose `message.content` actually changed. New fields are:
- `content_changed_visible_text_comparable_nodes`
- `content_changed_text_uncomparable_nodes`

It preserves `visible_text_changed_nodes` and `content_changed_but_visible_text_equal_nodes` with content-scoped semantics.

V4 also adds privacy-preserving structural carrier signatures for content-changed/text-uncomparable nodes. Signatures retain only representation types, dictionary keys, list lengths/item-type counts, and SHA-256 hashes of that structural shape. They do not retain message bodies or scalar values. Pair signatures are aggregated as `text_uncomparable_carrier_shape_pair_hash_counts`.

Commit creating v4: `312d39d36d0bd8b48c874569fffbbd0e822e91ab`.

## Checks / limits
Code inspection establishes the intended denominator and retention semantics. Production execution is **not yet established**. I attempted to update `.github/workflows/mercer-cross-source-integrity.yml` so pushes to v4 trigger the workflow and the conversation-identity step invokes v4; the connector safety layer blocked that workflow-file write. I did not bypass the guard. Therefore v3 remains the workflow-invoked production diagnostic at the end of this run.

This is an operational tooling limitation, not evidence of repository corruption and not a Nathan-required decision. Next run should either use an allowed execution path to test v4 or, if workflow editing remains unavailable, inspect whether another project-native runner/handoff can wire it without weakening write safety.

## What this establishes
- The Run-64 semantic defect now has a concrete versioned repair implementation.
- Non-text-comparable changed content can now be structurally distinguished without retaining body/scalar values once v4 is executed.
- No identity suppression/disposition semantics were added.
- No archive source, Viewer record, filename, or conversation identity was changed.

## Frontier
1. Execute v4 against the production-resolved corpus and inspect `ChatGPT Voice Glitch` first.
2. Verify corrected `AI Enclosure Critique` denominator/counts.
3. Only after production-green execution, use v4 structural signatures to classify the remaining text-uncomparable residue.
4. Continue ancestry reconstruction separately from equivalence classification.

## Workflow observation for Sable
Direct Contents-API modification of the GitHub Actions workflow was blocked by the connector safety layer. If v4 is retained, production wiring needs an allowed project-native path; do not weaken the existing fresh-base/read-only workflow contract to achieve it.

## Nathan attention
None.
