# Mercer Run 32 — 2026-09-14

## Control / boundaries

Read at run start:

- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`

Project-wide theory-bearing standdown remains active. This run stayed inside source/index/retrieval QA. No theory-bearing synthesis, source-content promotion, raw-source mutation, or duplicate disposition was performed.

## Operation chosen

Followed the Run-31 checkpoint frontier: locate the most stable existing machine-readable upstream Nathan Direct count surface before implementing another parser or scraping Markdown.

## Exact sources / regions covered

- `indexes/nathan-direct/` directory listing
- `indexes/nathan-direct/MANIFEST.json`
- `WORKSPACES/MERCER/CROSS_SOURCE_INTEGRITY_VALIDATOR_FEASIBILITY_2026-09-14.md`
- current Control/Common/Checkpoint surfaces listed above

## Finding

`indexes/nathan-direct/MANIFEST.json` is the stable machine-readable package-count bridge Run 31 was looking for. It exposes:

- input records: 69,927
- input user records: 21,451
- packaged unique user messages: 14,306
- collapsed duplicate user records: 7,145
- per-year shard record counts: 293 / 306 / 4,801 / 8,906
- missing-ID and graph-pointer counts

This allows deterministic package arithmetic and direct package → Stage-2 count continuity without parsing raw conversations or scraping `AUTOTAG_SUMMARY.md`.

Current arithmetic closes:

- 14,306 + 7,145 = 21,451
- 293 + 306 + 4,801 + 8,906 = 14,306
- existing Stage-2 `source_records` = 14,306, matching packaged unique user messages

This does **not** by itself establish autotag → package freshness/generation lineage; a stable machine-readable autotag-side state marker has not yet been confirmed, so that join remains `UNKNOWN`/manual rather than inferred from equal counts.

## Artifacts changed

Updated:

- `WORKSPACES/MERCER/CROSS_SOURCE_INTEGRITY_VALIDATOR_FEASIBILITY_2026-09-14.md`
  - added `indexes/nathan-direct/MANIFEST.json` as the preferred machine-readable Nathan Direct count adapter;
  - added safe package arithmetic invariants;
  - narrowed the remaining autotag bridge gap to source-generation lineage rather than package counts.

Commit: `4c5f2a72c6209fd921e318a8f9a84e841c3af82d`.

Created this run record.

## Current frontier

The v0 validator no longer needs to discover an upstream Nathan Direct package-count surface. Remaining useful implementation work can begin with existing metadata adapters:

1. Viewer ↔ date-manifest freshness/arithmetic/path checks;
2. manifest blocker/status arithmetic;
3. Nathan Direct package arithmetic and package ↔ Stage-2 `source_records` continuity.

Autotag → Nathan Direct generation-state continuity remains a separate optional bridge until a stable machine-readable autotag state surface is identified.

## Blockers / dependencies

- `OWNER ACTION`: SAT_CONVOS_15 exact duplicate disposition remains external to Mercer.
- `DEPENDENCY`: canonical Viewer/navigation regeneration remains stale until owner execution lands.
- `DEPENDENCY`: machine-readable autotag-side generation/state lineage surface not yet confirmed.
- `AMBIGUOUS_PROVENANCE`: glossary/crosswalk raw-message ancestry remains closed pending stronger anchors.
- No Nathan-required decision surfaced.

## Common coordination

No new Common handoff was warranted. This run closes an internal validator-design bridge without creating a new cross-lane dependency.

## Best next operation

Implement the minimal read-only validator over the already verified Viewer/date-manifest/Nathan-Direct metadata surfaces, emitting deterministic `PASS/WARN/BLOCKED/FAIL/UNKNOWN` JSON plus a compact Markdown summary. Do not expand into raw archive parsing to fill optional identity fields.