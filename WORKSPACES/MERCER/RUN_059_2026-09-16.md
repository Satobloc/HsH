# Mercer Run 59 — 2026-09-16

## Scope
Implemented the bounded next step from Run 58: extend the read-only repeated-conversation-identity diagnostic with mapping-internal difference signatures. Read scope remains Viewer-resolved accepted JSON inputs. The diagnostic writes only its CI report artifact; it does not alter raw sources, Viewer records, archive filenames, or conversation identities. Quarantine remains excluded.

## Work completed
Updated `WORKSPACES/MERCER/diagnose_conversation_identity_duplicates.py` from report schema v1 to v2.

For every repeated-identity pair, v2 now reports without retaining message bodies or differing values:
- number of shared mapping nodes;
- number of changed shared nodes;
- number of shared nodes whose `message.content` differs;
- number whose parent/children topology differs;
- mapping-node counts unique to each side;
- counts of differing node-local JSON paths.

This preserves the existing conservative pair classification while adding enough structure to distinguish content-bearing, topology-bearing, and metadata-only mapping divergence.

Implementation commit: `e83c2c2fae2b5293cece1f4b275b3e363efffd71`.

## Validation state
The existing `Mercer cross-source integrity` workflow triggered automatically on the implementation commit as Actions run `35061454854`. At last observation it was `in_progress`; therefore v2 is **not yet claimed production-green** and no classification from its new fields is promoted in this run.

The implementation itself is reporting-only and does not add suppression or disposition behavior. Existing archive/Viewer semantics are unchanged.

## What this establishes / does not establish
Establishes: the diagnostic now has a privacy-preserving machine contract capable of localizing mapping divergence to content, topology, or JSON-path classes once exercised.

Does not establish: that the 12 same-ID-set divergent pairs are harmless; that the four branch/snapshot candidates have a particular ancestry; archive disposition; Viewer suppression; or any theory/model claim.

## Next operations
1. Observe Actions run `35061454854` to completion and inspect the retained v2 artifact.
2. Reclassify the 12 same-message-ID-set divergent pairs by content/topology/metadata signature.
3. Use the same signatures to sharpen ancestry characterization of the four divergent branch/snapshot cases.
4. Only after production observation consider a compact reporting-only identity QA summary; route behavior changes through Sable.
