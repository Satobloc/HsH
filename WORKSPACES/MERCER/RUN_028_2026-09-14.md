# Mercer Run 28 — 2026-09-14

## Scope

Bounded Viewer/generated-metadata documentation invariant under the active theory-bearing standdown. No theory-bearing synthesis performed.

## Startup/control surfaces read

- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`

Controlling state retained: Nathan directives control; standdown remains active; Mercer lane is index/retrieval QA, Nathan Direct methodology/source reconstruction, and documentation/navigation reconciliation. `MORROW-SOURCE-001` remains code-side resolved/output-side pending and was not reopened without a report owner/path.

## Exact sources/regions inspected

- `CONVERSATION_VIEWER/data/conversations.json` — catalog header, counts, and input metadata.
- `CONVERSATION_VIEWER/README.md` — Viewer scope/build documentation.
- `CONVERSATION_VIEWER/EXTERNAL_CONVERSATIONS.json` — complete current registry (one external conversation).
- `tools/build_conversation_viewer.py` — build defaults plus `build_manifest` external-registration loading/count semantics.

## Finding

The Viewer has **not** regenerated since Mercer's prior observation: `source_state_at_utc` remains `2026-09-13T12:30:33.868040+00:00`, so the stale-owner-generation dependency is unchanged and was not polled further.

A separate documentation mismatch was verified. The Viewer README described the catalog as derived from development/live manifests and the Viewer as browsing those two archive families, but the current implementation also loads `CONVERSATION_VIEWER/EXTERNAL_CONVERSATIONS.json`.

Current arithmetic demonstrates why the distinction matters:

- development manifest accepted JSON conversations: **364**;
- live manifest accepted JSON conversations: **9**;
- registered external accepted conversations: **1**;
- the external record (`Srena — H(s)H`) declares `corpus: development`;
- resulting catalog counts: **365 development + 9 live = 374 conversations**.

The builder records the external input separately as `registered-external`, but top-level development/live counts are computed from each resulting conversation's declared `corpus`. Therefore top-level `development` is not necessarily identical to the development-manifest acceptance count.

## Change made

Updated `CONVERSATION_VIEWER/README.md` to:

1. state that the Viewer can browse explicitly registered external conversation sources in addition to `DEVELOPMENT_FULL_CONVOS` and `LIVE CONVOS`;
2. name `CONVERSATION_VIEWER/EXTERNAL_CONVERSATIONS.json` as a catalog input when present;
3. clarify that external registrations point to canonical source repositories rather than copying the source into HsH;
4. document the count semantics: a registered external record's declared `corpus` contributes to development/live top-level counts, while input metadata separately labels the source `registered-external`.

Commit: `0db8a9067a5bda081eec5f60032d4bd5adc35bb4`.

## Boundaries

- No raw conversation, generated catalog, manifest, curation rule, or external registration was changed.
- No stale Viewer owner workflow was repolled.
- No theory-bearing or quarantined material was interpreted or promoted.
- No Morrow continuity work or Nathan-words extraction/tagging work was duplicated.

## Common coordination

No new blocker or cross-lane dependency was created. The fix is local owning-documentation reconciliation, so no Common handoff was warranted.

## Current frontier

Viewer source generation remains stale at the previously recorded September 13 source state; reopen only on genuinely new owner-generated state. The external-source count/documentation mismatch is closed.

## Best next operation

Reread Control/Common. Do not recheck this external-count invariant unless the registry/build semantics change. If Viewer source state advances, compare catalog inputs/counts/source paths against the regenerated manifests. Otherwise choose a different bounded retrieval/index/generated-metadata invariant with measurable repository evidence; leave exhausted glossary ancestry, reconciled corpus-count delta, and reportless `MORROW-SOURCE-001` output dependency closed/pending respectively.
