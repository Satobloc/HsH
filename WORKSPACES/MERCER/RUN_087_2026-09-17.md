# Mercer Run 87 — formalization `leancheck` history trace

**Date:** 2026-09-17  
**Class:** bounded archive/documentation provenance QA  
**Theory work:** none  
**Quarantine exposure:** none

## Question
Run 86 established that `formalization/README.md` advertises `formalization/leancheck/run.ps1` and `formalization/leancheck/README.md`, while the path is absent from current `main`. This run asked the narrower provenance question: was the advertised wrapper ever actually materialized in repository history?

## Exact coverage
- current `formalization/README.md`;
- commit history for `formalization/README.md`;
- commit `c4e6bfa6e848a165ee2c1761b1c85a5328cba2af` (`Add Windows Lean check wrapper`, 2026-09-14T16:32:00Z);
- that commit's changed-file list/patch;
- direct lookup of `formalization/leancheck/run.ps1` at that commit;
- path-specific commit history for `formalization/leancheck/run.ps1`.

## Result
The missing subordinate path is not presently explained by a later deletion.

Commit `c4e6bfa6e848a165ee2c1761b1c85a5328cba2af`, whose message is `Add Windows Lean check wrapper`, changed only `formalization/README.md` (31 additions / 3 deletions). Its patch added the prose that advertises `formalization/leancheck/run.ps1`, `.work/`, and `formalization/leancheck/README.md`; the commit's changed-file list contains no wrapper/script files.

Direct retrieval of `formalization/leancheck/run.ps1` at that exact commit returns 404. GitHub's path-specific commit history for `formalization/leancheck/run.ps1` returns an empty list.

### What this establishes
Under GitHub's repository/path history visible in this audit, the advertised `formalization/leancheck/run.ps1` was **not committed at the commit that introduced the documentation and has no recorded commit history at that path**. The strongest current classification is therefore **documented-but-unmaterialized wrapper path**, rather than `later removed wrapper`.

### What this does not establish
This does not establish whether the wrapper existed locally, in an unmerged branch, in another repository/path, or in an unrecorded working tree. It does not establish whether Lean itself has been run successfully by some other route. It says nothing about mathematical or model correctness.

## Archive/infrastructure significance
The Run-86 repair question is now narrower. Before changing the README, the next useful operation is to search for the wrapper by distinctive command/content signatures or alternate paths/branches. If none exists, route a small owner decision: either materialize the intended wrapper or mark/remove the unsupported README instructions. Do not infer which remedy was intended from the commit message alone.

## Next cursor
Search HsH history/current tree for distinctive wrapper strings (`SkipCache`, `RefreshMathlib`, `.work`, `lake env lean`) and any alternate PowerShell path before proposing documentation repair.
