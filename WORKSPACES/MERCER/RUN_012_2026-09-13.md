# Mercer Run 12 — 2026-09-13

**Scope:** bounded navigation/source-integrity QA under the active theory standdown.

## Startup / controlling surfaces

Read at run start:
- `WORKSPACES/COMMON/AUTOMATION_WORKFLOW_CONTROL.md`
- `WORKSPACES/COMMON/BULLETIN_BOARD.md`
- `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md`
- current canonical navigation workflow and Viewer QA record

No newer Nathan directive released the theory standdown or displaced Mercer's navigation/source-integrity lane.

## Work performed

1. Rechecked canonical navigation ownership after commit `44548450fa4af2905f711e81ce07139b393983d6`.
2. Confirmed `.github/workflows/maintain-navigation.yml` still contains the repaired behavior: it invokes `tools/build_conversation_viewer_resolved.py` and asserts every non-external Viewer source path exists.
3. Inspected recent repository history after the repair. Multiple ordinary non-`[skip hsh-maintenance]` commits have landed since the patch, but no newer `[skip hsh-maintenance] Maintain date/index/tag/viewer navigation` commit is present.
4. Checked recent commit check-runs where available; the latest inspected ordinary commit exposed no attached check runs. This does not establish workflow failure, only that no successful owner regeneration is presently visible through the inspected surfaces.
5. Re-read the canonical development manifest directly. Its header is still `generated_at_utc: 2026-09-13T12:30:33.434526+00:00`.
6. Confirmed the manifest still physically contains `DEVELOPMENT_FULL_CONVOS/SAT_CONVOS_6/25.12.29•25.12.30•Court Filing Guidance — raw.json` as an unchanged 78-message record.
7. Confirmed the corresponding repository file path returns `404 Not Found` on current `main`.

## Result

The Viewer repair remains correct, but the upstream development manifest has still not been refreshed by a visibly successful canonical owner run. The stale deleted-source record therefore remains a **generation-lag dependency**, not a basis for hand-editing generated state.

No evidence in this run changes the prior conclusion that the generator itself should drop the dead record when it next executes successfully against current `main`.

## Artifacts changed

- created this run note;
- updated `WORKSPACES/MERCER/VIEWER_PATH_QA_2026-09-13.md` with the Run 12 verification state;
- advanced `WORKSPACES/MERCER/TRIAL_CHECKPOINT.md` through Run 12.

## Current frontier

Wait for a demonstrably successful canonical `Maintain H(s)H navigation` regeneration. On the first such state:
- verify the development manifest timestamp advances;
- verify `Court Filing Guidance` disappears from the manifest;
- verify the Viewer source-path assertion passes;
- close the Viewer/manifest branch if all three hold.

If a successful owner regeneration still retains the dead record, diagnose the generator/workflow source-first. If no owner success is visible next run, branch to another permitted provenance/documentation task rather than repeatedly rechecking the same dependency.

## Blockers / Nathan attention

- `DEPENDENCY`: successful owner regeneration not yet visible.
- No Nathan decision is required.
