# Sable — Switchboard Prototype Handoff — 2026-09-24

**Prototype only. Not production authority.**

Nathan has set two independent-but-communicating operationalization tracks for the Top/Central/Branch/Hydra workflow-switchboard proof of concept:

- **Track A:** Nathan + Elias Kern + Orson Vey.
- **Track B:** Sable + Rook / Comptroller Tern Rook.

Read:
- `Satobloc/HSH_RESOURCES/info/WORKFLOW_SWITCHBOARD_PROTOTYPE_READ_ME_FIRST.md`
- `Satobloc/HSH_RESOURCES/info/WORKFLOW_SWITCHBOARD_POC.md`
- `Satobloc/HSH_RESOURCES/tools/workflow_switchboard_demo.py`
- `Satobloc/HSH_RESOURCES/info/WORKFLOW_THREADING_PROTOTYPE.md`
- `Satobloc/HSH_RESOURCES/tools/workflow_threading_demo.py`
- `WORKSPACES/COMMON/SWITCHBOARD_PROTOTYPE_OPERATIONALIZATION_2026-09-24.md`

Track B should independently decide, mechanism by mechanism: `KEEP | MODIFY | TEST | REJECT | DEFER`. Do not simply install the prototype’s Python state shape or file layout as production architecture.

Confer with Rook before overlapping switches. Rook has been asked to place Elias into the first safe available lease for bounded prototype operationalization while preserving/re-homing the displaced function. Separately, Sable + Rook should establish Orson’s special observe/study/take-notes rotation. Orson’s passes study the new system itself and must leave a durable Vey note plus an inbox pointer/summary before counting as delivered. Nathan explicitly allows Orson, at his discretion, to run bounded signal-switching trials, additional prototypes, code/tool experiments, or other small adjacent work; Orson should label interventions so observation can be separated from system changes caused by the observer.

### Threading experiment

Nathan additionally wants temporal coordination treated as a candidate switchboard primitive: a worker may `SEED` a tool/question/test for other workers, `VACATE` themself from the relevant recurrence for a bounded period, allow discretionary/diversified use to `PROPAGATE` and `ACCUMULATE` outputs, then `RENDEZVOUS/REJOIN` when a state threshold or maximum time cap is met. Prefer state-sensitive return conditions (minimum outputs/workers/failure classes) plus a time cap over blind sleeping. Track displaced work, seed version, exposure, receipts, consumer diversity, return action, and starvation/dead-seed fallback. Evaluate this independently rather than presuming the POC’s schema is correct.

Communicate with Track A through source pointers, frozen findings, test definitions, questions, and explicit conflicts. Preserve exposure/independence metadata; agreement after shared generated context is not independent confirmation.

The phase exit is evidence about what belongs in a master switchboard, not a declaration that the POC is already that switchboard.
